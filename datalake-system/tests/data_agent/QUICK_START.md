# Data Agent 測試劇本快速開始

**創建日期**：2026-01-13
**創建人**：Daniel Chung

## 快速執行

### 1. 確保服務運行

```bash
# 檢查 Data Agent 服務狀態
cd /Users/daniel/GitHub/AI-Box/datalake-system
./scripts/data_agent/status.sh
```

如果服務未運行，先啟動：

```bash
./scripts/data_agent/start.sh
```

### 2. 執行測試

```bash
# 方法 1：使用測試腳本（推薦）
./tests/data_agent/run_tests.sh

# 方法 2：直接運行 Python
python3 tests/data_agent/test_data_agent_scenarios.py
```

### 3. 查看結果

測試完成後會顯示：

- ✅ 通過的測試數
- ❌ 失敗的測試數
- 📊 通過率

詳細結果保存在：`tests/data_agent/test_results.json`

查看詳細結果：

```bash
cat tests/data_agent/test_results.json | python3 -m json.tool
```

## 測試場景總覽

本測試劇本包含 **28 個測試場景**：

### 功能測試（正常流程）

1. Datalake 精確查詢
2. Datalake 模糊查詢
3. Datalake 查詢（帶過濾條件）
4. 創建數據字典
5. 獲取數據字典
6. 創建 JSON Schema
7. 獲取 Schema
8. 驗證有效數據
9. Text-to-SQL 簡單查詢
10. Text-to-SQL 複雜查詢
11. 驗證有效的 SQL
12. 創建 Schema（帶引用）
13. 驗證空列表數據

### 錯誤處理測試

14. Datalake 查詢 - 缺少 bucket
15. Datalake 查詢 - 缺少 key
16. 獲取不存在的數據字典
17. 創建數據字典 - 缺少數據
18. 獲取不存在的 Schema
19. 創建無效的 Schema
20. 驗證無效數據
21. 驗證數據 - 缺少 Schema ID
22. Text-to-SQL - 缺少自然語言
23. 驗證無效的 SQL
24. 驗證危險的 SQL
25. 未知操作
26. 創建數據字典 - 重複 ID

### 邊界情況測試

27. Datalake 查詢 - 空 bucket
28. Datalake 查詢 - 大量結果

## 預期結果

### 正常情況

- ✅ 功能測試應該全部通過
- ✅ 錯誤處理測試應該正確檢測錯誤
- ⚠️  某些測試可能因環境配置而失敗（如文件不存在），這是正常的

### 失敗情況

如果大量測試失敗，請檢查：

1. **服務是否運行**：

   ```bash
   ./scripts/data_agent/status.sh
   ```

2. **環境配置是否正確**：

   ```bash
   python3 scripts/check_environment.py
   ```

3. **SeaweedFS 服務是否正常**：

   ```bash
   docker ps | grep seaweedfs-datalake
   ```

## 自定義測試

如需添加自定義測試，請編輯：

```
tests/data_agent/test_data_agent_scenarios.py
```

參考現有測試函數的格式添加新測試。

## 問題排查

### 問題 1：ModuleNotFoundError

**解決方案**：

```bash
cd /Users/daniel/GitHub/AI-Box/datalake-system
./scripts/data_agent/install_dependencies.sh
```

### 問題 2：服務連接失敗

**解決方案**：

1. 檢查服務是否運行：`./scripts/data_agent/status.sh`
2. 檢查環境變數：`python3 scripts/check_environment.py`
3. 重啟服務：`./scripts/data_agent/restart.sh`

### 問題 3：測試執行緩慢

**原因**：某些測試需要等待服務響應

**解決方案**：這是正常的，測試會自動等待響應

## 聯繫

如有問題，請查看：

- [測試腳本 README](./README.md)
- [Datalake System README](../../README.md)
