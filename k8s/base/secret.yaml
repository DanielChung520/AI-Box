# 代碼功能說明: Kubernetes Secret 配置示例（請使用實際的 secret 管理工具）
# 創建日期: 2025-10-25
# 創建人: Daniel Chung
# 最後修改日期: 2025-11-25

# 注意：此文件僅作為示例，實際使用時應使用 kubectl create secret 或外部 secret 管理工具
# 示例：
# kubectl create secret generic ai-box-secret \
#   --from-literal=secret-key=your-secret-key \
#   --from-literal=jwt-secret-key=your-jwt-secret-key \
#   --namespace=ai-box

apiVersion: v1
kind: Secret
metadata:
  name: ai-box-secret
  namespace: ai-box
type: Opaque
stringData:
  secret-key: "change-me-in-production"
  jwt-secret-key: "change-me-in-production"
