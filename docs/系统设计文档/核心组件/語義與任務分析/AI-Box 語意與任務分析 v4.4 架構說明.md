# AI-Box 語意與任務分析 v4.4 架構說明

**代碼功能說明**: AI-Box 語意與任務分析系統的最終整合說明文件
**創建日期**: 2026-02-20
**創建人**: Daniel Chung
**版本**: v4.4

---

## 📋 文檔概述

本文檔整合所有語意與任務分析的設計與實現，澄清 v4.3 以來的混淆，提供清晰的架構指導。

### 版本歷史

| 版本 | 日期 | 說明 |
|------|------|------|
| v4.0 | 2025-xx-xx | 初始版本 |
| v4.1 | 2025-xx-xx | 重構版本 |
| v4.2 | 2026-01-xx | 新增 Task Analyzer |
| v4.3 | 2026-02-09 | 兩層意圖分類架構 |
| v4.4 | 2026-02-20 | 澄清端點解析，整合實現狀態 |

---

## 🏗️ 系統架構

### 整體流程圖

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           用戶輸入 (AI-Box Frontend)                         │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                    第一層：AI-Box API Router (chat.py)                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ GAI Intent Classification (classify_gai_intent)                    │   │
│  │  - GREETING, THANKS, COMPLAIN, CANCEL, CONTINUE, MODIFY             │   │
│  │  - HISTORY, EXPORT, CONFIRM, FEEDBACK, CLARIFICATION, BUSINESS     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                      │                                     │
│              ┌───────────────────────┼───────────────────────┐             │
│              ▼                       ▼                       ▼             │
│       GREETING/etc.          CLARIFICATION            BUSINESS             │
│       (直接回覆)              (請求澄清)              (轉發 BPA)          │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼ (當 GAIIntent = BUSINESS)
┌─────────────────────────────────────────────────────────────────────────────┐
│                    第二層：MM-Agent (BPA) - Port 8003                      │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │ BPA Intent Classification (intent_endpoint.py)                       │   │
│  │  - KNOWLEDGE_QUERY → KA-Agent (8000)                               │   │
│  │  - SIMPLE_QUERY → Data-Agent (8004)                                │   │
│  │  - COMPLEX_TASK → ReAct Planner + RQ Worker                        │   │
│  │  - CLARIFICATION → 返回澄清                                        │   │
│  │  - CONTINUE_WORKFLOW → 繼續執行工作流                              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                      │
              ┌───────────────────────┼───────────────────────┐
              ▼                       ▼                       ▼
        KA-Agent              Data-Agent              RQ Worker
        (8000)                (8004)                  (異步)
```

---

## 🔄 兩層意圖分類架構

### 第一層：AI-Box GAI 前端意圖

**職責**：在 AI-Box API 層直接處理對話管理相關意圖

**代碼位置**：`api/routers/chat.py:33-291`

```python
class GAIIntentType(str, Enum):
    GREETING = "GREETING"        # 問候/打招呼
    CLARIFICATION = "CLARIFICATION"  # 需要澄清
    CANCEL = "CANCEL"            # 取消任務
    CONTINUE = "CONTINUE"        # 繼續執行
    MODIFY = "MODIFY"            # 重新處理
    HISTORY = "HISTORY"          # 顯示歷史
    EXPORT = "EXPORT"            # 導出結果
    CONFIRM = "CONFIRM"          # 確認回覆
    THANKS = "THANKS"            # 感謝回覆
    COMPLAIN = "COMPLAIN"        # 道歉處理
    FEEDBACK = "FEEDBACK"        # 記錄反饋
    BUSINESS = "BUSINESS"         # 業務請求（轉發 BPA）
```

**實現狀態**：✅ 完整實現

| 意圖 | 狀態 | 說明 |
|------|------|------|
| GREETING | ✅ 已實現 | 問候回覆 |
| THANKS | ✅ 已實現 | 感謝回覆 |
| COMPLAIN | ✅ 已實現 | 道歉處理 |
| CANCEL | ✅ 已實現 | 終止任務 |
| CONTINUE | ✅ 已實現 | 繼續執行 |
| MODIFY | ✅ 已實現 | 重新處理 |
| HISTORY | ✅ 已實現 | 顯示歷史 |
| EXPORT | ✅ 已實現 | 導出結果 |
| CONFIRM | ✅ 已實現 | 請求確認 |
| FEEDBACK | ✅ 已實現 | 記錄反饋 |
| CLARIFICATION | ✅ 已實現 | 請求澄清 |
| BUSINESS | ✅ 已實現 | 轉發 BPA |

### 第二層：MM-Agent (BPA) 業務意圖

**職責**：在 MM-Agent 服務中進行業務意圖分類

**代碼位置**：`datalake-system/mm_agent/intent_endpoint.py`

```python
class BPAIntentType(str, Enum):
    KNOWLEDGE_QUERY = "KNOWLEDGE_QUERY"      # 業務知識問題
    SIMPLE_QUERY = "SIMPLE_QUERY"            # 簡單數據查詢
    COMPLEX_TASK = "COMPLEX_TASK"            # 複雜任務/操作指引
    CLARIFICATION = "CLARIFICATION"          # 需要澄清
    CONTINUE_WORKFLOW = "CONTINUE_WORKFLOW"  # 繼續執行工作流
```

**實現狀態**：✅ 完整實現

| 意圖 | 下游處理 | 說明 |
|------|----------|------|
| KNOWLEDGE_QUERY | KA-Agent (8000) | 知識庫查詢 |
| SIMPLE_QUERY | Data-Agent (8004) | SQL 查詢 |
| COMPLEX_TASK | ReAct Planner | 複雜任務 |
| CLARIFICATION | 返回澄清 | 需要更多資訊 |
| CONTINUE_WORKFLOW | 執行下一步 | 繼續工作流 |

---

## 🔌 Agent 端點解析

### 設計原則

AI-Box 調用外部 Agent（如 MM-Agent）時，需要解析其 endpoint URL。有兩種配置來源：

| 來源 | 用途 | 實現狀態 |
|------|------|----------|
| **Agent Registry** | 內存註冊，運行時發現 | ✅ 已實現 |
| **agent_display_configs** | 持久化配置，前端展示 | ❌ 未實現 |

### 當前問題

**`agent_display_configs` 集合不存在**：

- 設計文檔指定應該通過 `agent_display_configs` 集合來存儲 Agent 配置
- 遷移腳本 `scripts/migration/create_schema.py` 定義了這個集合，但從未被執行
- 導致代碼中的解析邏輯失敗，需要臨時 workaround

### 解決方案

#### 方案 A：執行遷移腳本（推薦）

```bash
# 執行遷移腳本建立集合
python scripts/migration/create_schema.py

# Seed mm-agent 配置
```

#### 方案 B：使用 Agent Registry

修改代碼，使用 `get_agent_registry()` 解析 endpoint：

```python
from agents.services.registry.registry import get_agent_registry

registry = get_agent_registry()
agent_info = registry.get_agent_info("mm-agent")

if agent_info and agent_info.endpoints:
    mm_endpoint = agent_info.endpoints.http
```

### Agent ID 映射

| 前端顯示名稱 | agent_id | _key (ArangoDB) | endpoint |
|-------------|----------|-----------------|----------|
| 經寶物料管理代理 | mm-agent | -h0tjyh | http://localhost:8003/api/v1/chat/auto-execute |

**當前代碼邏輯** (`chat.py:2243-2315`)：

```python
# 1. 中文名稱 → agent_id
AGENT_NAME_TO_ID_MAP = {
    "經寶物料管理代理": "mm-agent",
}

# 2. _key → agent_id (如果前端傳遞 "-h0tjyh")
# 查詢 agent_display_configs

# 3. agent_id → _key (反向解析)
# 查詢 agent_display_configs
```

---

## 📊 實際代碼流程

### 完整處理流程

```python
# api/routers/chat.py - _process_chat_request()

# Step 1: GAI 意圖分類
gai_intent = classify_gai_intent(last_user_text)

if gai_intent != GAIIntentType.BUSINESS:
    # 直接回覆，不轉發
    return direct_gai_response(gai_intent, last_user_text)

# Step 2: 判斷是否轉發
should_forward = should_forward_to_bpa(
    text=last_user_text,
    gai_intent=gai_intent,
    has_selected_agent=has_selected_agent,
    agent_id=user_selected_agent_id,
)

# Step 3: 解析 Agent Endpoint
if is_mm_agent or should_forward:
    # 嘗試從 agent_display_configs 獲取 endpoint
    store = AgentDisplayConfigStoreService()
    agent_config = store.get_agent_config(agent_key=selected_agent)
    
    if agent_config and agent_config.endpoint_url:
        mm_endpoint = agent_config.endpoint_url
    
    # 或者使用硬編碼 fallback
    mm_endpoint = "http://localhost:8003/api/v1/chat/auto-execute"

# Step 4: 調用 MM-Agent
response = httpx.post(mm_endpoint, json=mm_request, timeout=120.0)
```

---

## 📁 關鍵代碼位置

| 功能 | 文件位置 | 行號 |
|------|----------|------|
| GAIIntentType 定義 | `api/routers/chat.py` | 33-52 |
| classify_gai_intent() | `api/routers/chat.py` | 67-291 |
| should_forward_to_bpa() | `api/routers/chat.py` | 369-393 |
| Agent 解析邏輯 | `api/routers/chat.py` | 2243-2315 |
| MM-Agent 轉發 | `api/routers/chat.py` | 2371-2470 |
| BPAIntentType 定義 | `datalake-system/mm_agent/intent_endpoint.py` | - |
| Intent 分類端點 | `datalake-system/mm_agent/main.py` | - |

---

## 🔧 待處理問題

### 高優先級

| 問題 | 嚴重程度 | 狀態 | 解決方案 |
|------|----------|------|----------|
| agent_display_configs 集合不存在 | 🔴 高 | 待處理 | 執行 create_schema.py |
| Agent 端點解析 fallback | 🔴 高 | 待處理 | 使用 Agent Registry |
| 中文名稱映射硬編碼 | 🟡 中 | 待處理 | 從配置載入 |

### 中優先級

| 問題 | 嚴重程度 | 狀態 | 解決方案 |
|------|----------|------|----------|
| 前端 agent_id 傳遞格式 | 🟡 中 | 待確認 | 確認是名稱/_key/agent_id |
| 測試覆蓋 | 🟢 低 | 待完善 | 添加集成測試 |

---

## 📚 參考文檔

### 當前有效文檔

- **本文檔**: `語義與任務分析/AI-Box 語意與任務分析 v4.4 架構說明.md`

### 歸檔文檔

已移至 `archive/` 目錄：

- `AI-Box，語意與任務分發v4.3架構更新計劃.md` → `archive/`
- `語義與任務分析詳細說明.md` → `archive/`
- `AI-Box 語義與任務工程-設計說明書-v4.md` → `archive/`
- `AI-Box語義與任務v4重構計劃.md` → `archive/`
- `15_指代消解邏輯.md` → `archive/`
- `14_Entity_Memory_規格書.md` → `archive/`
- `14_Entity_Memory_實作計畫.md` → `archive/`
- `文件編輯Agent語義路由測試計劃-v4.md` → `archive/`

---

## 📝 更新記錄

| 日期 | 版本 | 更新內容 | 更新人 |
|------|------|----------|--------|
| 2026-02-20 | v4.4 | 創建整合文檔，澄清端點解析，歸檔舊文檔 | Daniel Chung |
| 2026-02-09 | v4.3 | 兩層意圖分類架構 | Daniel Chung |

---

**文檔版本**: v4.4
**最後更新**: 2026-02-20
**維護人**: Daniel Chung
