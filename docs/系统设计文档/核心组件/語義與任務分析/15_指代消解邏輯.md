# 指代消解邏輯設計文檔

**文檔版本**: v1.0  
**創建日期**: 2026-02-04  
**最後修改日期**: 2026-02-04  
**作者**: AI-Box 團隊

---

## 1. 概述

### 1.1 定義

指代消解（Coreference Resolution）是自然語言處理中的一項技術，用於識別文本中指代同一實體的不同表達方式。在 AI-Box 系統中，指代消解主要用於：

- 識別用戶對話中提及的實體（如料號、訂單等）
- 將實體名稱與其含義關聯存儲
- 在後續對話中自動補全上下文信息

### 1.2 設計目標

- **簡單實用**：採用簡潔的 key-value 存儲結構
- **會話隔離**：實體信息在單個會話內有效
- **無縫整合**：與現有的 MM-Agent 系統整合

---

## 2. 系統架構

### 2.1 組件關係

```
┌─────────────────────────────────────────────────────────────┐
│                      前端 (AI-Bot)                          │
│  ┌─────────────────────────────────────────────────────┐  │
│  │                   Home.tsx                          │  │
│  │  ┌─────────────┐    ┌─────────────┐               │  │
│  │  │ 指代消解模組 │───▶│   Task 類型  │               │  │
│  │  └─────────────┘    └─────────────┘               │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                    MM-Agent 服務                            │
│  ┌─────────────────────────────────────────────────────┐  │
│  │              semantic_analyzer.py                   │  │
│  │              positive_list.py                       │  │
│  └─────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 數據流向

```
用戶輸入: "RM01-009 庫存有多少？"
         │
         ▼
┌─────────────────────────────────────┐
│  指代消解模組                        │
│  1. 檢查 Task.entities             │
│  2. 匹配並補全                       │
│  3. 輸出: "料號 RM01-009 的庫存有多少？"
└─────────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────────┐
│  MM-Agent 服務                       │
│  接收完整指令，執行庫存查詢            │
└─────────────────────────────────────┘
```

---

## 3. 數據結構

### 3.1 Task 類型擴展

```typescript
// 文件: components/Sidebar.tsx

export interface Task {
  id: number | string;
  title: string;
  status: 'pending' | 'in-progress' | 'completed';
  task_status?: 'activate' | 'archive' | 'trash';
  dueDate: string;
  messages?: Message[];
  // 指代消解實體（2026-02-04）
  entities?: Record<string, string>;  // { "RM01-009": "料號", "ABC-123": "訂單" }
  executionConfig?: {
    mode: 'free' | 'assistant' | 'agent';
    assistantId?: string;
    agentId?: string;
    modelId?: string;
    sessionId?: string;
  };
  fileTree?: FileNode[];
}
```

### 3.2 實體存儲示例

```typescript
// 會話開始時
entities = {}

// 用戶輸入: "RM01-009 是一個料號"
entities = {
  "RM01-009": "料號"
}

// 用戶輸入: "ABC 是訂單"
entities = {
  "RM01-009": "料號",
  "ABC": "訂單"
}

// 用戶輸入: "料號 RM01-009 的庫存有多少？"
// → 指代消解後輸出
output = "料號 RM01-009 的庫存有多少？"
```

---

## 4. 實現邏輯

### 4.1 指代提取模式

系統支持以下實體提取模式：

| 模式 | 正則表達式 | 示例 | 動作 |
|------|-----------|------|------|
| X 是 Y | `^(.+?)\s*(是\|為\|等於)\s*(.+)$` | "RM01-009 是料號" | `entities["RM01-009"] = "料號"` |
| X = Y | `^(.+?)\s*=\s*(.+)$` | "RM01-009 = 料號" | `entities["RM01-009"] = "料號"` |

### 4.2 指代消解算法

```typescript
// 文件: pages/Home.tsx

interface EntityPattern {
  regex: RegExp;
  handler: (match: RegExpMatchArray) => { name: string; meaning: string };
}

// 步驟 1: 提取新實體
function extractEntity(instruction: string): { name: string; meaning: string } | null {
  const patterns = [
    /^(.+?)\s*(是|為|等於)\s*(.+)$/,  // "RM01-009 是料號"
    /^(.+?)\s*=\s*(.+)$/,              // "RM01-009 = 料號"
  ];

  for (const pattern of patterns) {
    const match = instruction.match(pattern);
    if (match) {
      const name = match[1].trim();
      const meaning = match[3].trim();
      // 排除無效對應
      if (name !== meaning && name.length > 1 && meaning.length > 1) {
        return { name, meaning };
      }
    }
  }
  return null;
}

// 步驟 2: 指代消解
function resolveCoreference(
  instruction: string,
  entities: Record<string, string>
): string {
  let resolved = instruction;
  for (const [key, meaning] of Object.entries(entities)) {
    // 使用正則避免部分匹配
    const regex = new RegExp(`\\b${key.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}\\b`, 'g');
    resolved = resolved.replace(regex, `${meaning} ${key}`);
  }
  return resolved;
}

// 步驟 3: 整合流程
async function processCoreference(
  instruction: string,
  currentEntities: Record<string, string>
): Promise<{
  resolvedMessage: string;
  newEntities: Record<string, string>;
}> {
  let newEntities = { ...currentEntities };
  let resolvedMessage = instruction;

  // 提取新實體
  const extracted = extractEntity(instruction);
  if (extracted) {
    newEntities[extracted.name] = extracted.meaning;
  }

  // 指代消解
  for (const [key, meaning] of Object.entries(newEntities)) {
    const regex = new RegExp(`\\b${key.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}\\b`, 'g');
    resolvedMessage = resolvedMessage.replace(regex, `${meaning} ${key}`);
  }

  return { resolvedMessage, newEntities };
}
```

### 4.3 完整指代消解流程

```typescript
// 文件: pages/Home.tsx

// 指代消解邏輯（2026-02-04）
const lastUserMessage = chatMessages[chatMessages.length - 1]?.content || '';
const currentEntities = taskWithUserMessage.entities || {};

// 1. 提取新實體
const entityPatterns = [
  /^(.+?)\s*(是|為|等於)\s*(.+)$/,  // "RM01-009 是料號"
  /^(.+?)\s*=\s*(.+)$/,              // "RM01-009 = 料號"
];

let newEntities = { ...currentEntities };
let resolvedMessage = lastUserMessage;

for (const pattern of entityPatterns) {
  const match = lastUserMessage.match(pattern);
  if (match) {
    const name = match[1].trim();
    const meaning = match[3].trim();
    if (name !== meaning && name.length > 1 && meaning.length > 1) {
      newEntities[name] = meaning;
      console.log('[指代消解] 📝 提取實體:', { name, meaning });
    }
    break;
  }
}

// 2. 指代消解：將 entities 中的 key 替換為 "意義 key"
for (const [key, meaning] of Object.entries(newEntities)) {
  const regex = new RegExp(`\\b${key.replace(/[.*+?^${}()|[\]\\]/g, '\\$&')}\\b`, 'g');
  resolvedMessage = resolvedMessage.replace(regex, `${meaning} ${key}`);
}

// 3. 如果消息被修改，更新消息
if (resolvedMessage !== lastUserMessage) {
  console.log('[指代消解] 🔄 消解後:', resolvedMessage);
  const updatedMessages = chatMessages.map((msg, idx) => {
    if (idx === chatMessages.length - 1 && msg.role === 'user') {
      return { ...msg, content: resolvedMessage };
    }
    return msg;
  });
  requestParams.messages = updatedMessages;
}

// 4. 保存 entities 到 task
const taskWithEntities: Task = {
  ...taskWithUserMessage,
  entities: newEntities,
};

console.log('[指代消解] 📚 當前 entities:', newEntities);
```

---

## 5. 與 MM-Agent 的整合

### 5.1 消息流程

```
用戶: "RM01-009 是一個料號"
  │
  ▼
前端指代消解模組
  │
  ├── 提取: entities["RM01-009"] = "料號"
  │
  ▼
保存到 Task.entities
  │
  ▼
用戶: "RM01-009 庫存有多少？"
  │
  ▼
前端指代消解模組
  │
  ├── 匹配: "RM01-009" → "料號 RM01-009"
  │
  ▼
發送給 MM-Agent: "料號 RM01-009 的庫存有多少？"
  │
  ▼
MM-Agent 理解這是庫存查詢，返回庫存信息
```

### 5.2 MM-Agent 配置

MM-Agent 需要支持 `general_chat` 任務類型：

```python
# 文件: datalake-system/mm_agent/positive_list.py

POSITIVE_KEYWORDS = [
    # ... 現有關鍵詞 ...
    # 記憶/記錄相關（2026-02-04 新增）
    "記住",
    "記錄",
    "記得",
]
```

---

## 6. 使用示例

### 6.1 基本用法

```typescript
// 示例 1: 記住料號
用戶輸入: "RM01-009 是一個料號"
系統輸出: "好的，我已記住 RM01-009 是一個料號"
entities: { "RM01-009": "料號" }

// 示例 2: 查詢庫存（使用指代消解）
用戶輸入: "RM01-009 庫存有多少？"
消解後: "料號 RM01-009 的庫存有多少？"
系統輸出: "RM01-009 當前庫存為 1,593,348 件"
```

### 6.2 多實體管理

```typescript
// 示例: 記住多個實體
用戶輸入: "RM01-009 是料號"
entities: { "RM01-009": "料號" }

用戶輸入: "ABC 是訂單"
entities: { "RM01-009": "料號", "ABC": "訂單" }

用戶輸入: "RM01-009 庫存有多少？"
消解後: "料號 RM01-009 的庫存有多少？"

用戶輸入: "ABC 狀態如何？"
消解後: "訂單 ABC 的狀態如何？"
```

---

## 7. 限制與擴展

### 7.1 當前限制

1. **會話隔離**：實體信息在會話結束後清除
2. **簡單匹配**：不支持復雜的指代表達（如代詞）
3. **順序依賴**：需要先"記住"才能"消解"

### 7.2 未來擴展方向

1. **長期記憶**：將實體存儲到數據庫，跨會話保持
2. **複雜指代**：支持"它"、"那個"等代詞消解
3. **同義詞擴展**：支持實體的別名和同義詞
4. **LLM 消解**：使用 LLM 處理複雜的指代關係

---

## 8. 測試用例

### 8.1 指代提取測試

```typescript
// 測試用例
const testCases = [
  {
    input: "RM01-009 是一個料號",
    expected: { name: "RM01-009", meaning: "料號" }
  },
  {
    input: "ABC 是訂單",
    expected: { name: "ABC", meaning: "訂單" }
  },
  {
    input: "RM01-009 = 料號",
    expected: { name: "RM01-009", meaning: "料號" }
  },
];
```

### 8.2 指代消解測試

```typescript
// 測試用例
const testCases = [
  {
    entities: { "RM01-009": "料號" },
    input: "RM01-009 庫存有多少？",
    expected: "料號 RM01-009 的庫存有多少？"
  },
  {
    entities: { "RM01-009": "料號", "ABC": "訂單" },
    input: "RM01-009 和 ABC",
    expected: "料號 RM01-009 和 訂單 ABC"
  },
];
```

---

## 9. 相關文檔

- [14_Entity_Memory_實作計畫.md](./14_Entity_Memory_實作計畫.md)
- [14_Entity_Memory_規格書.md](./14_Entity_Memory_規格書.md)
- [AI意圖分析與任務路由-人類語言規範.md](../AI意圖分析與任務路由-人類語言規範.md)
- [語義與任務分析詳細說明.md](./語義與任務分析詳細說明.md)

---

## 10. 版本歷史

| 版本 | 日期 | 變更 |
|------|------|------|
| v1.0 | 2026-02-04 | 初始版本 |
