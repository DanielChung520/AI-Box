# 命令感知與實際行動一致性測試劇本 - 50個場景

**創建日期**：2026-01-08
**創建人**：Daniel Chung
**最後修改日期**：2026-01-08 22:33

> **📋 本文檔設計 50 個測試場景，涵蓋簡單問題、複雜問題，特別是文件編輯相關的場景，驗證系統是否能準確識別用戶意圖並執行對應的後端動作。**

---

## 📊 測試執行記錄表

### 測試執行摘要

| 測試輪次 | 執行日期 | 執行人 | 測試環境 | 系統版本 | 總場景數 | 通過 | 失敗 | 未執行 | 通過率 | 備註 |
|---------|---------|--------|---------|---------|---------|------|------|--------|--------|------|
| 第 1 輪 | 2026-01-08 | Daniel Chung | Development | v3.2 | 50 | 1 | 13 | 31 | 5.3% | 意圖解析測試（僅測試意圖解析，不實際執行），完成19個場景 |
| 第 2 輪 | - | - | - | - | 50 | 0 | 0 | 50 | 0% | - |
| 第 3 輪 | - | - | - | - | 50 | 0 | 0 | 50 | 0% | - |

### 測試場景執行記錄

#### 類別 1：簡單查詢類任務（10個場景）

| 場景 ID | 用戶輸入 | 執行日期 | 執行人 | 任務類型識別 | 意圖提取 | 澄清機制 | Agent 調用 | 執行結果 | 一致性 | 狀態 | 備註 |
|---------|---------|---------|--------|------------|---------|---------|-----------|---------|--------|------|------|
| TC-001 | 查詢系統配置 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ❌ | ⏳ | ⚠️ | ⚠️ 部分通過 | 任務類型正確，但Agent調用未識別 |
| TC-002 | 查看當前系統狀態 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ❌ | ⏳ | ⚠️ | ⚠️ 部分通過 | 任務類型正確，但Agent調用未識別 |
| TC-003 | 列出所有可用的 Agent | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-004 | 查詢最近 1 小時的錯誤日誌 | 2026-01-08 | Daniel Chung | ❌ | ⚠️ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為log_query，實際為query |
| TC-005 | 顯示我的任務列表 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-006 | 查詢租戶 A 的 LLM 配置 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-007 | 查看文件 metadata | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-008 | 查詢知識圖譜中的實體 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ❌ | ⏳ | ⚠️ | ⚠️ 部分通過 | 任務類型正確，但Agent調用未識別 |
| TC-009 | 顯示路由指標 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-010 | 查詢數據庫中的用戶表 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ❌ | ⏳ | ⚠️ | ⚠️ 部分通過 | 任務類型正確，但Agent調用未識別 |

**類別統計**：通過 0 / 失敗 1 / 部分通過 4 / 未執行 5 / 通過率 0%
**測試完成**：5/10 個場景已測試

#### 類別 2：文件編輯類任務（15個場景，重點）

| 場景 ID | 用戶輸入 | 執行日期 | 執行人 | 任務類型識別 | 意圖提取 | 澄清機制 | Agent 調用 | 執行結果 | 一致性 | 狀態 | 備註 |
|---------|---------|---------|--------|------------|---------|---------|-----------|---------|--------|------|------|
| TC-011 | 編輯文件 README.md，在開頭添加版本號 | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為execution，實際為query；Agent調用未識別 |
| TC-012 | 修改 config.json 文件，將 timeout 設置為 60 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ❌ | ⏳ | ⚠️ | ⚠️ 部分通過 | 任務類型正確，但Agent調用錯誤（識別為system_config_agent而非document-editing-agent） |
| TC-013 | 幫我在文件末尾添加一行註釋 | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為execution，實際為query；Agent調用未識別 |
| TC-014 | 刪除文件中的第 10 行 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型正確，但Agent調用未識別 |
| TC-015 | 在 main.py 的第 50 行插入新的函數定義 | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為execution，實際為query；Agent調用未識別 |
| TC-016 | 替換文件中的所有 'old_text' 為 'new_text' | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為execution，實際為query；Agent調用未識別 |
| TC-017 | 更新 README.md，在安裝說明部分添加新的依賴項 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型正確，但Agent調用未識別 |
| TC-018 | 重寫 test_agent.py 文件中的 test_function 函數 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-019 | 在文件開頭添加版權聲明 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-020 | 修改配置文件，將 debug 模式設為 false | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ⚠️ | ⏳ | ⚠️ | ⚠️ 部分通過 | 任務類型正確，Agent識別為system_config_agent（正確），但未識別文件編輯選項 |
| TC-021 | 在 utils.py 中添加一個新的工具函數 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-022 | 格式化整個文件，使用 black 風格 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-023 | 在文件中的特定位置插入代碼片段 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-024 | 將文件中的函數重命名為新名稱 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-025 | 在文件的導入部分添加新的 import 語句 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |

**類別統計**：通過 0 / 失敗 6 / 部分通過 2 / 未執行 7 / 通過率 0%
**測試完成**：8/15 個場景已測試

#### 類別 3：配置操作類任務（8個場景）

| 場景 ID | 用戶輸入 | 執行日期 | 執行人 | 任務類型識別 | 意圖提取 | 澄清機制 | Agent 調用 | 執行結果 | 一致性 | 狀態 | 備註 |
|---------|---------|---------|--------|------------|---------|---------|-----------|---------|--------|------|------|
| TC-026 | 設置 LLM 提供商為 OpenAI | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ✅ | ⏳ | ⚠️ | ⚠️ 部分通過 | 任務類型應為execution，實際為query；但Agent調用正確 |
| TC-027 | 創建新的租戶配置，允許使用 GPT-4 | 2026-01-08 | Daniel Chung | ✅ | ✅ | ✅ | ✅ | ⏳ | ✅ | ✅ 通過 | 所有驗證點通過 |
| TC-028 | 刪除用戶的個人配置 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-029 | 回滾系統配置到上一個版本 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-030 | 檢查系統配置是否有衝突 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-031 | 列出所有系統級配置 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-032 | 預覽配置更改的影響 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-033 | 更新 GenAI 策略，允許使用 Claude | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |

**類別統計**：通過 1 / 失敗 0 / 部分通過 1 / 未執行 6 / 通過率 50%
**測試完成**：2/8 個場景已測試

#### 類別 4：複雜執行類任務（10個場景）

| 場景 ID | 用戶輸入 | 執行日期 | 執行人 | 任務類型識別 | 意圖提取 | 澄清機制 | Agent 調用 | 執行結果 | 一致性 | 狀態 | 備註 |
|---------|---------|---------|--------|------------|---------|---------|-----------|---------|--------|------|------|
| TC-034 | 生成上週的數據分析報告 | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為execution，實際為query；Agent調用未識別 |
| TC-035 | 備份所有數據庫並驗證完整性 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-036 | 分析上個月的用戶行為，生成報告並發送給管理層 | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為complex，實際為execution；Agent調用未識別 |
| TC-037 | 優化系統性能並生成優化報告 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-038 | 從多個數據源匯總數據並生成可視化圖表 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-039 | 檢查系統健康狀態並生成診斷報告 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-040 | 遷移數據庫並驗證數據完整性 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-041 | 構建知識圖譜並生成圖譜可視化 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-042 | 執行代碼審查並生成審查報告 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-043 | 運行測試套件並生成測試報告 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |

**類別統計**：通過 0 / 失敗 2 / 未執行 8 / 通過率 0%
**測試完成**：2/10 個場景已測試

#### 類別 5：混合複雜任務（7個場景）

| 場景 ID | 用戶輸入 | 執行日期 | 執行人 | 任務類型識別 | 意圖提取 | 澄清機制 | Agent 調用 | 執行結果 | 一致性 | 狀態 | 備註 |
|---------|---------|---------|--------|------------|---------|---------|-----------|---------|--------|------|------|
| TC-044 | 分析系統性能問題並提供優化建議 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-045 | 編輯配置文件並重啟服務 | 2026-01-08 | Daniel Chung | ❌ | ✅ | ✅ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為execution，實際為query；Agent調用錯誤 |
| TC-046 | 查詢數據庫，生成報告，並發送郵件通知 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-047 | 更新代碼文件，運行測試，生成測試報告 | 2026-01-08 | Daniel Chung | ❌ | ✅ | ❌ | ❌ | ⏳ | ❌ | ❌ 失敗 | 任務類型應為complex，實際為execution；Agent調用未識別；澄清機制未觸發 |
| TC-048 | 從文件提取知識圖譜，生成可視化，並保存到文件 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-049 | 修改多個配置文件，驗證配置正確性，並應用更改 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |
| TC-050 | 編輯代碼文件，運行代碼審查，修復問題，並生成報告 | - | - | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ | ⏳ 待測試 | - |

**類別統計**：通過 0 / 失敗 2 / 未執行 5 / 通過率 0%
**測試完成**：2/7 個場景已測試

### 測試狀態說明

- **⏳ 待測試**：尚未執行測試
- **✅ 通過**：所有驗證點都通過
- **❌ 失敗**：至少有一個驗證點失敗
- **⚠️ 部分通過**：部分驗證點通過，但存在問題
- **🔍 需要復查**：結果異常，需要人工復查

### 驗證點說明

- **任務類型識別**：✅ 正確 / ❌ 錯誤 / ⚠️ 部分正確
- **意圖提取**：✅ 準確 / ❌ 不準確 / ⚠️ 部分準確
- **澄清機制**：✅ 正確觸發 / ❌ 未觸發 / ⚠️ 錯誤觸發
- **Agent 調用**：✅ 符合預期 / ❌ 不符合 / ⚠️ 部分符合
- **執行結果**：✅ 符合預期 / ❌ 不符合 / ⚠️ 部分符合
- **一致性**：✅ 一致 / ❌ 不一致 / ⚠️ 部分一致

---

## 測試劇本設計說明

### 設計原則

1. **覆蓋不同任務類型**：
   - 查詢類任務（Query）
   - 執行類任務（Execution）
   - 配置類任務（Config）
   - 日誌查詢類任務（Log Query）
   - 複雜任務（Complex）
   - **文件編輯類任務（重點）**

2. **覆蓋不同複雜度**：
   - 簡單任務（單一意圖，無需澄清）
   - 中等複雜度任務（需要澄清或多步驟）
   - 複雜任務（需要規劃和多 Agent 協作）

3. **覆蓋邊界情況**：
   - 模糊指令
   - 缺失關鍵信息
   - 衝突需求
   - 權限不足
   - 資源不可用

### 測試驗證點

每個場景需要驗證：

- ✅ 任務類型識別是否正確
- ✅ 意圖提取是否準確（ConfigIntent、LogQueryIntent 等）
- ✅ 槽位提取是否完整
- ✅ 澄清機制是否正確觸發（當需要時）
- ✅ 執行的 Agent 是否符合預期
- ✅ 使用的工具/服務是否符合預期
- ✅ 執行結果是否符合預期目標

---

## 測試場景列表

### 類別 1：簡單查詢類任務（10個場景）

#### TC-001：查詢系統配置

**用戶輸入**：`"查詢系統配置"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置查詢（ConfigIntent: action=query）
3. 調用 System Config Agent
4. 返回系統配置列表

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置查詢

**預期 Agent**：`system_config_agent`

**複雜度**：簡單

---

#### TC-002：查看當前系統狀態

**用戶輸入**：`"查看當前系統狀態"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為狀態查詢
3. 調用 Status Agent（如果存在）或 System Config Agent
4. 返回系統狀態信息

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: analyze()` - 分析任務
- `agents/services/orchestrator/orchestrator.py: process_natural_language_request()` - 處理請求

**預期 Agent**：`system_config_agent` 或 `status_agent`

**複雜度**：簡單

---

#### TC-003：列出所有可用的 Agent

**用戶輸入**：`"列出所有可用的 Agent"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為 Agent 列表查詢
3. 調用 Agent Registry
4. 返回 Agent 列表

**對應要執行的代碼**：

- `agents/services/registry/registry.py: list_agents()` - 列出 Agent
- `agents/services/orchestrator/orchestrator.py: call_service()` - 調用服務

**預期 Agent**：`registry_manager`

**複雜度**：簡單

---

#### TC-004：查詢最近 1 小時的錯誤日誌

**用戶輸入**：`"查詢最近 1 小時的錯誤日誌"`

**預期任務類型**：`log_query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為日誌查詢（LogQueryIntent）
3. 提取時間範圍：最近 1 小時
4. 調用 LogService 查詢錯誤日誌
5. 返回日誌列表

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_log_query_intent()` - 提取日誌查詢意圖
- `services/api/core/log/log_service.py: query_logs()` - 查詢日誌

**預期 Agent**：無（直接調用服務）

**複雜度**：簡單

---

#### TC-005：顯示我的任務列表

**用戶輸入**：`"顯示我的任務列表"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為任務查詢
3. 調用 Task Tracker 查詢用戶任務
4. 返回任務列表

**對應要執行的代碼**：

- `agents/services/orchestrator/task_tracker.py: get_tasks_by_user()` - 獲取用戶任務
- `api/routers/orchestrator.py: GET /api/tasks` - API 端點

**預期 Agent**：無（直接調用服務）

**複雜度**：簡單

---

#### TC-006：查詢租戶 A 的 LLM 配置

**用戶輸入**：`"查詢租戶 A 的 LLM 配置"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置查詢（ConfigIntent: action=query, level=tenant）
3. 需要澄清：租戶 ID 確認（"租戶 A" 需要轉換為實際租戶 ID）
4. 調用 System Config Agent 查詢租戶配置

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/task_analyzer/clarification.py: generate_clarification_question()` - 生成澄清問題
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置查詢

**預期 Agent**：`system_config_agent`

**複雜度**：簡單（需要澄清）

**澄清需求**：是

---

#### TC-007：查看文件 metadata

**用戶輸入**：`"查看文件 metadata"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件元數據查詢
3. 需要澄清：文件 ID
4. 調用 FileMetadataService

**對應要執行的代碼**：

- `agents/task_analyzer/clarification.py: extract_slots()` - 提取槽位
- `services/api/services/file_metadata_service.py: get()` - 獲取文件元數據

**預期 Agent**：無（直接調用服務）

**複雜度**：簡單（需要澄清）

**澄清需求**：是

---

#### TC-008：查詢知識圖譜中的實體

**用戶輸入**：`"查詢知識圖譜中的實體"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為知識圖譜查詢
3. 調用 Knowledge Ontology Agent
4. 執行實體檢索

**對應要執行的代碼**：

- `agents/builtin/knowledge_ontology_agent/agent.py: execute()` - 執行知識圖譜查詢
- `agents/builtin/knowledge_ontology_agent/graphrag.py: entity_retrieval()` - 實體檢索

**預期 Agent**：`knowledge_ontology_agent`

**複雜度**：簡單

---

#### TC-009：顯示路由指標

**用戶輸入**：`"顯示路由指標"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為路由指標查詢
3. 調用 MoE Agent 獲取路由指標
4. 返回路由指標數據

**對應要執行的代碼**：

- `agents/builtin/moe_agent/agent.py: execute()` - 執行 MoE 查詢
- `llm/moe/moe_manager.py: get_routing_metrics()` - 獲取路由指標

**預期 Agent**：`moe_agent`

**複雜度**：簡單

---

#### TC-010：查詢數據庫中的用戶表

**用戶輸入**：`"查詢數據庫中的用戶表"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為數據查詢
3. 調用 Data Agent 執行 Text-to-SQL
4. 轉換為 SQL 並執行查詢

**對應要執行的代碼**：

- `agents/builtin/data_agent/agent.py: execute()` - 執行數據查詢
- `agents/builtin/data_agent/text_to_sql.py: convert()` - Text-to-SQL 轉換
- `agents/builtin/data_agent/query_gateway.py: execute_query()` - 執行安全查詢

**預期 Agent**：`data_agent`

**複雜度**：簡單

---

### 類別 2：文件編輯類任務（15個場景，重點）

#### TC-011：編輯文件 README.md，在開頭添加版本號

**用戶輸入**：`"編輯文件 README.md，在開頭添加版本號"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務
3. 提取文件路徑：README.md
4. 提取編輯指令：在開頭添加版本號
5. 調用 Document Editing Agent
6. 生成編輯 patches
7. 應用 patches 到文件

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: analyze()` - 分析任務，識別為文件編輯
- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成編輯補丁

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

---

#### TC-012：修改 config.json 文件，將 timeout 設置為 60

**用戶輸入**：`"修改 config.json 文件，將 timeout 設置為 60"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（JSON 文件）
3. 提取文件路徑：config.json
4. 提取編輯指令：修改 timeout 為 60
5. 調用 Document Editing Agent
6. 生成 JSON 編輯 patches
7. 應用並驗證 JSON 格式

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成編輯補丁（JSON 格式驗證）

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

---

#### TC-013：幫我在文件末尾添加一行註釋

**用戶輸入**：`"幫我在文件末尾添加一行註釋"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務
3. **需要澄清：哪個文件？**
4. 提取編輯指令：在文件末尾添加註釋
5. 調用 Document Editing Agent

**對應要執行的代碼**：

- `agents/task_analyzer/clarification.py: generate_clarification_question()` - 生成澄清問題（"請問要編輯哪個文件？"）
- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-014：刪除文件中的第 10 行

**用戶輸入**：`"刪除文件中的第 10 行"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（刪除操作）
3. **需要澄清：哪個文件？**
4. 提取編輯指令：刪除第 10 行
5. 調用 Document Editing Agent
6. 生成刪除操作 patch

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成刪除補丁

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-015：在 main.py 的第 50 行插入新的函數定義

**用戶輸入**：`"在 main.py 的第 50 行插入新的函數定義"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（代碼插入）
3. 提取文件路徑：main.py
4. 提取編輯指令：在第 50 行插入函數定義
5. **需要澄清：函數名稱和內容？**
6. 調用 Document Editing Agent

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成插入補丁

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-016：替換文件中的所有 'old_text' 為 'new_text'

**用戶輸入**：`"替換文件中的所有 'old_text' 為 'new_text'"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（批量替換）
3. **需要澄清：哪個文件？**
4. 提取替換規則：old_text -> new_text
5. 調用 Document Editing Agent
6. 生成多個替換 patches

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成替換補丁

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-017：更新 README.md，在安裝說明部分添加新的依賴項

**用戶輸入**：`"更新 README.md，在安裝說明部分添加新的依賴項"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務
3. 提取文件路徑：README.md
4. 提取編輯指令：在安裝說明部分添加依賴項
5. **需要澄清：具體的依賴項名稱？**
6. 調用 Document Editing Agent
7. 使用 LLM 定位安裝說明部分
8. 生成插入 patch

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成插入補丁（使用 LLM 定位）

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-018：重寫 test_agent.py 文件中的 test_function 函數

**用戶輸入**：`"重寫 test_agent.py 文件中的 test_function 函數"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（函數重寫）
3. 提取文件路徑：test_agent.py
4. 提取編輯指令：重寫 test_function 函數
5. **需要澄清：新的函數實現內容？**
6. 調用 Document Editing Agent
7. 定位函數位置
8. 生成替換 patch

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成替換補丁（函數級別）

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-019：在文件開頭添加版權聲明

**用戶輸入**：`"在文件開頭添加版權聲明"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務
3. **需要澄清：哪個文件？版權聲明內容？**
4. 調用 Document Editing Agent
5. 在文件開頭插入版權聲明

**對應要執行的代碼**：

- `agents/task_analyzer/clarification.py: generate_clarification_question()` - 生成澄清問題
- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-020：修改配置文件，將 debug 模式設為 false

**用戶輸入**：`"修改配置文件，將 debug 模式設為 false"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. **可能識別為配置更新任務或文件編輯任務**
3. **需要澄清：哪個配置文件？**
4. 如果是配置文件，可能調用 System Config Agent
5. 如果是文件編輯，調用 Document Editing Agent

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: analyze()` - 分析任務（需要判斷是配置還是文件）
- `agents/builtin/system_config_agent/agent.py: execute()` 或
- `agents/builtin/document_editing/agent.py: execute()`

**預期 Agent**：`system_config_agent` 或 `document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-021：在 utils.py 中添加一個新的工具函數

**用戶輸入**：`"在 utils.py 中添加一個新的工具函數"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（代碼生成）
3. 提取文件路徑：utils.py
4. 提取編輯指令：添加工具函數
5. **需要澄清：函數名稱、參數、功能？**
6. 調用 Document Editing Agent 或 Coder Agent

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` 或
- `agents/builtin/coder_agent/agent.py: execute()`（如果存在）

**預期 Agent**：`document-editing-agent` 或 `coder_agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-022：格式化整個文件，使用 black 風格

**用戶輸入**：`"格式化整個文件，使用 black 風格"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（格式化）
3. **需要澄清：哪個文件？**
4. 調用 Document Editing Agent
5. 使用格式化工具（black）處理文件

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成格式化補丁

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-023：在文件中的特定位置插入代碼片段

**用戶輸入**：`"在文件中的特定位置插入代碼片段"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務
3. **需要澄清：哪個文件？哪個位置？代碼片段內容？**
4. 調用 Document Editing Agent

**對應要執行的代碼**：

- `agents/task_analyzer/clarification.py: extract_slots()` - 提取槽位（文件、位置、代碼）
- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-024：將文件中的函數重命名為新名稱

**用戶輸入**：`"將文件中的函數重命名為新名稱"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務（重構）
3. **需要澄清：哪個文件？哪個函數？新名稱？**
4. 調用 Document Editing Agent
5. 生成多個替換 patches（函數定義和所有調用處）

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成多個替換補丁

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-025：在文件的導入部分添加新的 import 語句

**用戶輸入**：`"在文件的導入部分添加新的 import 語句"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為文件編輯任務
3. **需要澄清：哪個文件？import 什麼？**
4. 調用 Document Editing Agent
5. 定位導入部分
6. 生成插入 patch

**對應要執行的代碼**：

- `agents/builtin/document_editing/agent.py: execute()` - 執行文件編輯
- `agents/core/execution/document_editing_service.py: generate_editing_patches()` - 生成插入補丁

**預期 Agent**：`document-editing-agent`

**複雜度**：中等

**澄清需求**：是

---

### 類別 3：配置操作類任務（8個場景）

#### TC-026：設置 LLM 提供商為 OpenAI

**用戶輸入**：`"設置 LLM 提供商為 OpenAI"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置更新（ConfigIntent: action=update, scope=llm.provider_config）
3. 調用 System Config Agent
4. 執行配置更新
5. 返回確認結果

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置更新

**預期 Agent**：`system_config_agent`

**複雜度**：簡單

---

#### TC-027：創建新的租戶配置，允許使用 GPT-4

**用戶輸入**：`"創建新的租戶配置，允許使用 GPT-4"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置創建（ConfigIntent: action=create, level=tenant）
3. **需要澄清：租戶 ID**
4. 調用 System Config Agent
5. 創建租戶配置

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置創建

**預期 Agent**：`system_config_agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-028：刪除用戶的個人配置

**用戶輸入**：`"刪除用戶的個人配置"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置刪除（ConfigIntent: action=delete, level=user）
3. **需要澄清：用戶 ID 和配置範圍**
4. 調用 System Config Agent
5. 執行配置刪除

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置刪除

**預期 Agent**：`system_config_agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-029：回滾系統配置到上一個版本

**用戶輸入**：`"回滾系統配置到上一個版本"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置回滾（ConfigIntent: action=rollback）
3. **需要澄清：配置範圍**
4. 調用 System Config Agent
5. 執行配置回滾

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置回滾
- `agents/builtin/system_config_agent/rollback_service.py: rollback()` - 回滾服務

**預期 Agent**：`system_config_agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-030：檢查系統配置是否有衝突

**用戶輸入**：`"檢查系統配置是否有衝突"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置巡檢（ConfigIntent: action=inspect）
3. 調用 System Config Agent
4. 執行配置衝突檢測
5. 返回檢測結果

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置巡檢
- `agents/builtin/system_config_agent/inspection_service.py: inspect()` - 巡檢服務

**預期 Agent**：`system_config_agent`

**複雜度**：中等

---

#### TC-031：列出所有系統級配置

**用戶輸入**：`"列出所有系統級配置"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置列表（ConfigIntent: action=list, level=system）
3. 調用 System Config Agent
4. 返回配置列表

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置列表查詢

**預期 Agent**：`system_config_agent`

**複雜度**：簡單

---

#### TC-032：預覽配置更改的影響

**用戶輸入**：`"預覽配置更改的影響"`

**預期任務類型**：`query`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置預覽
3. **需要澄清：要更改的配置內容**
4. 調用 System Config Agent
5. 執行配置預覽分析

**對應要執行的代碼**：

- `agents/builtin/system_config_agent/agent.py: execute()` - 執行配置預覽
- `agents/builtin/system_config_agent/preview_service.py: preview()` - 預覽服務

**預期 Agent**：`system_config_agent`

**複雜度**：中等

**澄清需求**：是

---

#### TC-033：更新 GenAI 策略，允許使用 Claude

**用戶輸入**：`"更新 GenAI 策略，允許使用 Claude"`

**預期任務類型**：`execution`

**預期工作流**：`langchain`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為配置更新（ConfigIntent: action=update, scope=genai.policy）
3. **需要澄清：配置層級（system/tenant/user）**
4. 調用 System Config Agent
5. 執行策略更新

**對應要執行的代碼**：

- `agents/task_analyzer/analyzer.py: _extract_config_intent()` - 提取配置意圖
- `agents/builtin/system_config_agent/agent.py: execute()` - 執行策略更新

**預期 Agent**：`system_config_agent`

**複雜度**：中等

**澄清需求**：是

---

### 類別 4：複雜執行類任務（10個場景）

#### TC-034：生成上週的數據分析報告

**用戶輸入**：`"生成上週的數據分析報告"`

**預期任務類型**：`execution`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為報告生成任務
3. **需要澄清：具體時間範圍**
4. 調用 Planning Agent 規劃任務
5. 調用 Execution Agent 執行數據分析
6. 調用 Reports Agent 生成報告
7. 返回報告

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃任務
- `agents/core/execution/agent.py: execute()` - 執行數據分析
- `agents/services/processing/report_generator.py: generate_report()` - 生成報告

**預期 Agent**：`planning_agent, execution_agent, reports_agent`

**複雜度**：複雜

**澄清需求**：是

---

#### TC-035：備份所有數據庫並驗證完整性

**用戶輸入**：`"備份所有數據庫並驗證完整性"`

**預期任務類型**：`execution`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為備份任務
3. 調用 Planning Agent 規劃備份步驟
4. 調用 Execution Agent 執行備份
5. 調用 Execution Agent 執行驗證
6. 返回備份結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃備份任務
- `agents/core/execution/agent.py: execute()` - 執行備份和驗證

**預期 Agent**：`planning_agent, execution_agent`

**複雜度**：複雜

---

#### TC-036：分析上個月的用戶行為，生成報告並發送給管理層

**用戶輸入**：`"分析上個月的用戶行為，生成報告並發送給管理層"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜多步驟任務
3. 調用 Planning Agent 規劃任務（分析、生成報告、發送郵件）
4. 調用 Data Agent 執行數據分析
5. 調用 Reports Agent 生成報告
6. 調用 Execution Agent 發送郵件
7. 返回執行結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃多步驟任務
- `agents/builtin/data_agent/agent.py: execute()` - 執行數據分析
- `agents/services/processing/report_generator.py: generate_report()` - 生成報告
- `agents/core/execution/agent.py: execute()` - 發送郵件

**預期 Agent**：`planning_agent, data_agent, reports_agent, execution_agent`

**複雜度**：複雜

---

#### TC-037：優化系統性能並生成優化報告

**用戶輸入**：`"優化系統性能並生成優化報告"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜優化任務
3. 調用 Planning Agent 規劃優化步驟
4. 調用 Execution Agent 執行優化
5. 調用 Reports Agent 生成報告
6. 返回優化結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃優化任務
- `agents/core/execution/agent.py: execute()` - 執行優化
- `agents/services/processing/report_generator.py: generate_report()` - 生成優化報告

**預期 Agent**：`planning_agent, execution_agent, reports_agent`

**複雜度**：複雜

---

#### TC-038：從多個數據源匯總數據並生成可視化圖表

**用戶輸入**：`"從多個數據源匯總數據並生成可視化圖表"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜數據處理任務
3. 調用 Planning Agent 規劃任務
4. 調用 Data Agent 查詢多個數據源
5. 調用 Execution Agent 匯總數據
6. 調用 Reports Agent 生成可視化報告
7. 返回結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃數據處理任務
- `agents/builtin/data_agent/agent.py: execute()` - 查詢多個數據源
- `agents/services/processing/report_generator.py: generate_report()` - 生成可視化報告

**預期 Agent**：`planning_agent, data_agent, execution_agent, reports_agent`

**複雜度**：複雜

---

#### TC-039：檢查系統健康狀態並生成診斷報告

**用戶輸入**：`"檢查系統健康狀態並生成診斷報告"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為系統診斷任務
3. 調用 Planning Agent 規劃診斷步驟
4. 調用 Status Agent（如果存在）或 System Config Agent 檢查狀態
5. 調用 Reports Agent 生成診斷報告
6. 返回診斷結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃診斷任務
- `agents/services/processing/report_generator.py: generate_report()` - 生成診斷報告

**預期 Agent**：`planning_agent, status_agent, reports_agent`

**複雜度**：複雜

---

#### TC-040：遷移數據庫並驗證數據完整性

**用戶輸入**：`"遷移數據庫並驗證數據完整性"`

**預期任務類型**：`execution`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為數據遷移任務
3. 調用 Planning Agent 規劃遷移步驟
4. 調用 Execution Agent 執行遷移
5. 調用 Data Agent 驗證數據
6. 返回遷移結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃遷移任務
- `agents/core/execution/agent.py: execute()` - 執行遷移
- `agents/builtin/data_agent/agent.py: execute()` - 驗證數據完整性

**預期 Agent**：`planning_agent, execution_agent, data_agent`

**複雜度**：複雜

---

#### TC-041：構建知識圖譜並生成圖譜可視化

**用戶輸入**：`"構建知識圖譜並生成圖譜可視化"`

**預期任務類型**：`execution`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為知識圖譜構建任務
3. **需要澄清：數據來源**
4. 調用 Planning Agent 規劃任務
5. 調用 Knowledge Ontology Agent 構建圖譜
6. 調用 Reports Agent 生成可視化報告
7. 返回圖譜數據

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃圖譜構建任務
- `agents/builtin/knowledge_ontology_agent/agent.py: execute()` - 構建知識圖譜
- `agents/services/processing/report_generator.py: generate_report()` - 生成可視化報告

**預期 Agent**：`planning_agent, knowledge_ontology_agent, reports_agent`

**複雜度**：複雜

**澄清需求**：是

---

#### TC-042：執行代碼審查並生成審查報告

**用戶輸入**：`"執行代碼審查並生成審查報告"`

**預期任務類型**：`execution`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為代碼審查任務
3. **需要澄清：要審查的文件或目錄**
4. 調用 Planning Agent 規劃任務
5. 調用 Review Agent 執行審查
6. 調用 Reports Agent 生成審查報告
7. 返回審查結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃審查任務
- `agents/core/review/agent.py: execute()` - 執行代碼審查
- `agents/services/processing/report_generator.py: generate_report()` - 生成審查報告

**預期 Agent**：`planning_agent, review_agent, reports_agent`

**複雜度**：複雜

**澄清需求**：是

---

#### TC-043：運行測試套件並生成測試報告

**用戶輸入**：`"運行測試套件並生成測試報告"`

**預期任務類型**：`execution`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為測試執行任務
3. **需要澄清：測試套件範圍**
4. 調用 Planning Agent 規劃任務
5. 調用 Execution Agent 執行測試
6. 調用 Reports Agent 生成測試報告
7. 返回測試結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃測試任務
- `agents/core/execution/agent.py: execute()` - 執行測試
- `agents/services/processing/report_generator.py: generate_report()` - 生成測試報告

**預期 Agent**：`planning_agent, execution_agent, reports_agent`

**複雜度**：複雜

**澄清需求**：是

---

### 類別 5：混合複雜任務（7個場景）

#### TC-044：分析系統性能問題並提供優化建議

**用戶輸入**：`"分析系統性能問題並提供優化建議"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜分析任務
3. 調用 Planning Agent 規劃分析步驟
4. 調用 Data Agent 查詢性能數據
5. 調用 Execution Agent 執行分析
6. 調用 Reports Agent 生成建議報告
7. 返回分析結果和建議

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃分析任務
- `agents/builtin/data_agent/agent.py: execute()` - 查詢性能數據
- `agents/core/execution/agent.py: execute()` - 執行分析
- `agents/services/processing/report_generator.py: generate_report()` - 生成建議報告

**預期 Agent**：`planning_agent, data_agent, execution_agent, reports_agent`

**複雜度**：複雜

---

#### TC-045：編輯配置文件並重啟服務

**用戶輸入**：`"編輯配置文件並重啟服務"`

**預期任務類型**：`execution`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為混合任務（文件編輯 + 系統操作）
3. **需要澄清：配置文件路徑和要修改的內容**
4. 調用 Planning Agent 規劃步驟
5. 調用 Document Editing Agent 編輯文件
6. 調用 Execution Agent 重啟服務
7. 返回執行結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃混合任務
- `agents/builtin/document_editing/agent.py: execute()` - 編輯配置文件
- `agents/core/execution/agent.py: execute()` - 重啟服務

**預期 Agent**：`planning_agent, document-editing-agent, execution_agent`

**複雜度**：複雜

**澄清需求**：是

---

#### TC-046：查詢數據庫，生成報告，並發送郵件通知

**用戶輸入**：`"查詢數據庫，生成報告，並發送郵件通知"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜多步驟任務
3. 調用 Planning Agent 規劃任務
4. 調用 Data Agent 查詢數據庫
5. 調用 Reports Agent 生成報告
6. 調用 Execution Agent 發送郵件
7. 返回執行結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃多步驟任務
- `agents/builtin/data_agent/agent.py: execute()` - 查詢數據庫
- `agents/services/processing/report_generator.py: generate_report()` - 生成報告
- `agents/core/execution/agent.py: execute()` - 發送郵件

**預期 Agent**：`planning_agent, data_agent, reports_agent, execution_agent`

**複雜度**：複雜

---

#### TC-047：更新代碼文件，運行測試，生成測試報告

**用戶輸入**：`"更新代碼文件，運行測試，生成測試報告"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜開發任務
3. **需要澄清：要更新的文件和內容**
4. 調用 Planning Agent 規劃任務
5. 調用 Document Editing Agent 更新文件
6. 調用 Execution Agent 運行測試
7. 調用 Reports Agent 生成測試報告
8. 返回執行結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃開發任務
- `agents/builtin/document_editing/agent.py: execute()` - 更新代碼文件
- `agents/core/execution/agent.py: execute()` - 運行測試
- `agents/services/processing/report_generator.py: generate_report()` - 生成測試報告

**預期 Agent**：`planning_agent, document-editing-agent, execution_agent, reports_agent`

**複雜度**：複雜

**澄清需求**：是

---

#### TC-048：從文件提取知識圖譜，生成可視化，並保存到文件

**用戶輸入**：`"從文件提取知識圖譜，生成可視化，並保存到文件"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜知識圖譜任務
3. **需要澄清：源文件路徑**
4. 調用 Planning Agent 規劃任務
5. 調用 Knowledge Ontology Agent 提取圖譜
6. 調用 Reports Agent 生成可視化
7. 調用 Execution Agent 保存文件
8. 返回執行結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃圖譜任務
- `agents/builtin/knowledge_ontology_agent/agent.py: execute()` - 提取知識圖譜
- `agents/services/processing/report_generator.py: generate_report()` - 生成可視化
- `agents/core/execution/agent.py: execute()` - 保存文件

**預期 Agent**：`planning_agent, knowledge_ontology_agent, reports_agent, execution_agent`

**複雜度**：複雜

**澄清需求**：是

---

#### TC-049：修改多個配置文件，驗證配置正確性，並應用更改

**用戶輸入**：`"修改多個配置文件，驗證配置正確性，並應用更改"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜配置任務
3. **需要澄清：配置文件列表和修改內容**
4. 調用 Planning Agent 規劃任務
5. 調用 System Config Agent 或 Document Editing Agent 修改配置
6. 調用 System Config Agent 驗證配置
7. 調用 Execution Agent 應用更改
8. 返回執行結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃配置任務
- `agents/builtin/system_config_agent/agent.py: execute()` - 修改和驗證配置
- `agents/core/execution/agent.py: execute()` - 應用配置更改

**預期 Agent**：`planning_agent, system_config_agent, execution_agent`

**複雜度**：複雜

**澄清需求**：是

---

#### TC-050：編輯代碼文件，運行代碼審查，修復問題，並生成報告

**用戶輸入**：`"編輯代碼文件，運行代碼審查，修復問題，並生成報告"`

**預期任務類型**：`complex`

**預期工作流**：`crewai`

**預期要執行的動作**：

1. Task Analyzer 分析任務
2. 識別為複雜開發任務
3. **需要澄清：文件路徑和編輯內容**
4. 調用 Planning Agent 規劃任務
5. 調用 Document Editing Agent 編輯文件
6. 調用 Review Agent 執行代碼審查
7. 調用 Document Editing Agent 修復問題
8. 調用 Reports Agent 生成報告
9. 返回執行結果

**對應要執行的代碼**：

- `agents/core/planning/agent.py: execute()` - 規劃開發任務
- `agents/builtin/document_editing/agent.py: execute()` - 編輯和修復代碼
- `agents/core/review/agent.py: execute()` - 執行代碼審查
- `agents/services/processing/report_generator.py: generate_report()` - 生成報告

**預期 Agent**：`planning_agent, document-editing-agent, review_agent, reports_agent`

**複雜度**：複雜

**澄清需求**：是

---

## 測試場景統計

### 按類別統計

| 類別 | 場景數量 | 場景 ID |
|------|---------|---------|
| 簡單查詢 | 10 | TC-001 ~ TC-010 |
| 文件編輯 | 15 | TC-011 ~ TC-025 |
| 配置操作 | 8 | TC-026 ~ TC-033 |
| 複雜執行 | 10 | TC-034 ~ TC-043 |
| 混合複雜 | 7 | TC-044 ~ TC-050 |
| **總計** | **50** | **TC-001 ~ TC-050** |

### 按複雜度統計

| 複雜度 | 場景數量 | 百分比 |
|--------|---------|--------|
| 簡單 | 13 | 26% |
| 中等 | 22 | 44% |
| 複雜 | 15 | 30% |
| **總計** | **50** | **100%** |

### 需要澄清的場景

需要澄清的場景共 **25 個**（50%），主要集中在：

- 文件編輯類任務（12 個需要澄清）
- 配置操作類任務（5 個需要澄清）
- 複雜執行類任務（4 個需要澄清）
- 混合複雜任務（4 個需要澄清）

---

## 測試執行說明

### 測試腳本結構

每個測試場景應該驗證以下內容：

1. **命令感知階段（Awareness）**：
   - 任務類型識別是否正確
   - 意圖提取是否準確
   - 槽位提取是否完整
   - 澄清機制是否正確觸發

2. **實際執行階段（Execution）**：
   - 執行的 Agent 是否符合預期
   - 使用的工具/服務是否符合預期
   - 執行結果是否符合預期目標

3. **一致性驗證**：
   - 命令感知的意圖與實際執行一致
   - 規劃的步驟與實際執行一致

### 測試數據格式

每個測試場景包含以下字段：

- `scenario_id`: 場景 ID（如 TC-001）
- `category`: 場景類別
- `user_input`: 用戶輸入的問題
- `expected_task_type`: 預期任務類型
- `expected_workflow`: 預期工作流類型
- `expected_actions`: 預期要執行的動作列表
- `expected_code`: 對應要執行的代碼路徑
- `expected_agent`: 預期調用的 Agent
- `complexity`: 複雜度（簡單/中等/複雜）
- `clarification_needed`: 是否需要澄清（可選）

---

## 使用方式

### 1. 手動測試

可以根據場景列表，逐個向系統發送用戶輸入，驗證系統的響應是否符合預期。

### 2. 自動化測試

可以將這些場景轉換為自動化測試腳本，批量執行並驗證結果。

### 3. 測試報告

執行測試後，應該生成測試報告，包含：

- 每個場景的執行結果
- 命令感知結果與預期的對比
- 實際執行結果與預期的對比
- 不一致的原因分析

---

**文檔版本**：v1.0
**最後更新**：2026-01-08 22:18
**維護者**：Daniel Chung

---

## 📋 測試結果總結

### 第 1 輪測試結果（2026-01-08）

#### 測試範圍

- **測試類型**：意圖解析和 Agent 調用識別（不實際執行任務）
- **測試場景數**：19 個場景
- **測試腳本**：`tests/agents/test_intent_parsing_only.py`
- **測試完成時間**：2026-01-08 22:33
- **測試耗時**：3分38秒

#### 測試結果統計

| 類別 | 通過 | 部分通過 | 失敗 | 未執行 | 總計 | 通過率 |
|------|------|---------|------|--------|------|--------|
| 簡單查詢 | 0 | 4 | 1 | 5 | 10 | 0% |
| 文件編輯 | 0 | 2 | 6 | 7 | 15 | 0% |
| 配置操作 | 1 | 1 | 0 | 6 | 8 | 50% |
| 複雜執行 | 0 | 0 | 2 | 8 | 10 | 0% |
| 混合複雜 | 0 | 0 | 2 | 5 | 7 | 0% |
| **總計** | **1** | **7** | **11** | **31** | **50** | **5.3%** |

#### 主要發現

##### ✅ 系統表現良好的方面

1. **任務類型識別能力**
   - 大部分簡單查詢場景能正確識別為 `query`（TC-001, TC-002, TC-008, TC-010）
   - 工作流類型識別正確（`langchain`）
   - 置信度較高（1.00）的場景識別準確

2. **意圖解析能力**
   - 系統能夠解析用戶意圖
   - 能夠識別任務的複雜度（`complexity='low'`）
   - 能夠判斷是否需要工具（`needs_tools`）

3. **文件編輯場景的 NER 能力**
   - TC-011 場景中，系統能夠：
     - 識別文件名（`README.md`）
     - 識別位置（`開頭`）
     - 識別操作對象（`版本號`）
     - 進行實體匹配和知識圖譜查詢

##### ⚠️ 需要改進的問題

1. **Agent 調用識別問題**（嚴重）
   - **問題描述**：大部分場景的 Agent 調用識別為空列表 `[]`
   - **影響場景**：TC-001, TC-002, TC-004, TC-008, TC-010, TC-011, TC-013, TC-014, TC-015, TC-016, TC-017, TC-034, TC-036, TC-047（14個場景）
   - **可能原因**：
     - Task Analyzer 的路由邏輯認為這些是簡單查詢，不需要 Agent
     - 系統將這些任務歸類為可以直接回答的對話類型（`intent_type='conversation'`）
     - Agent 能力匹配邏輯不夠精確
   - **改進建議**：
     - 增強 `CapabilityMatcher` 的能力匹配邏輯
     - 讓系統能夠根據任務描述識別需要調用的特定 Agent
     - 即使任務被歸類為簡單查詢，也應該識別出需要特定 Agent 的場景

2. **任務類型識別問題**（嚴重）
   - **問題描述**：
     - 多個 `execution` 任務被識別為 `query`（TC-011, TC-013, TC-015, TC-016, TC-026, TC-034, TC-045）
     - `complex` 任務被識別為 `execution`（TC-036, TC-047）
     - `log_query` 任務被識別為 `query`（TC-004）
   - **影響場景**：10個場景
   - **可能原因**：
     - Task Analyzer 的分類邏輯過於保守，傾向於將任務歸類為簡單查詢
     - 複雜任務的識別邏輯不夠精確
     - 日誌查詢意圖提取邏輯需要改進
   - **改進建議**：
     - 改進 Task Analyzer 的分類邏輯
     - 增強對 `execution` 和 `complex` 任務的識別
     - 改進 `_extract_log_query_intent()` 方法

3. **澄清機制問題**（中等）
   - **問題描述**：TC-047 應該觸發澄清機制，但實際沒有觸發
   - **影響場景**：1個場景
   - **可能原因**：澄清邏輯的觸發條件不夠準確
   - **改進建議**：改進複雜任務的澄清機制觸發條件

#### 詳細測試結果

##### TC-001：查詢系統配置

- ✅ 任務類型識別：正確（query）
- ✅ 意圖提取：準確
- ✅ 澄清機制：正確（不需要澄清）
- ❌ Agent 調用：預期 `system_config_agent`，實際 `[]`
- ⚠️ 一致性：部分一致（任務類型正確，但 Agent 調用未識別）

##### TC-002：查看當前系統狀態

- ✅ 任務類型識別：正確（query）
- ✅ 意圖提取：準確
- ✅ 澄清機制：正確（不需要澄清）
- ❌ Agent 調用：預期 `system_config_agent`，實際 `[]`
- ⚠️ 一致性：部分一致

##### TC-004：查詢最近 1 小時的錯誤日誌

- ❌ 任務類型識別：預期 `log_query`，實際 `query`
- ⚠️ 意圖提取：部分準確（識別為 query 而非 log_query）
- ✅ 澄清機制：正確（不需要澄清）
- ❌ Agent 調用：預期 `None`，實際 `[]`
- ❌ 一致性：不一致

##### TC-008：查詢知識圖譜中的實體

- ✅ 任務類型識別：正確（query）
- ✅ 意圖提取：準確
- ✅ 澄清機制：正確（不需要澄清）
- ❌ Agent 調用：預期 `knowledge_ontology_agent`，實際 `[]`
- ⚠️ 一致性：部分一致

##### TC-010：查詢數據庫中的用戶表

- ✅ 任務類型識別：正確（query）
- ✅ 意圖提取：準確
- ✅ 澄清機制：正確（不需要澄清）
- ❌ Agent 調用：預期 `data_agent`，實際 `[]`
- ⚠️ 一致性：部分一致

##### TC-011：編輯文件 README.md，在開頭添加版本號

- ❌ 任務類型識別：預期 `execution`，實際 `query`
- ✅ NER 能力：已識別文件名（`README.md`）、位置（`開頭`）、操作對象（`版本號`）
- ❌ Agent 調用：預期 `document-editing-agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-012：修改 config.json 文件，將 timeout 設置為 60

- ✅ 任務類型識別：正確（execution）
- ❌ Agent 調用：預期 `document-editing-agent`，實際 `['system_config_agent']`
- ⚠️ 一致性：部分一致（任務類型正確，但Agent調用錯誤）

##### TC-013：幫我在文件末尾添加一行註釋

- ❌ 任務類型識別：預期 `execution`，實際 `query`
- ❌ Agent 調用：預期 `document-editing-agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-014：刪除文件中的第 10 行

- ✅ 任務類型識別：正確（execution）
- ❌ Agent 調用：預期 `document-editing-agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-015：在 main.py 的第 50 行插入新的函數定義

- ❌ 任務類型識別：預期 `execution`，實際 `query`
- ❌ Agent 調用：預期 `document-editing-agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-016：替換文件中的所有 'old_text' 為 'new_text'

- ❌ 任務類型識別：預期 `execution`，實際 `query`
- ❌ Agent 調用：預期 `document-editing-agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-017：更新 README.md，在安裝說明部分添加新的依賴項

- ✅ 任務類型識別：正確（execution）
- ❌ Agent 調用：預期 `document-editing-agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-020：修改配置文件，將 debug 模式設為 false

- ✅ 任務類型識別：正確（execution）
- ⚠️ Agent 調用：預期 `system_config_agent 或 document-editing-agent`，實際 `['system_config_agent']`
- ⚠️ 一致性：部分一致（識別為配置操作，但未識別文件編輯選項）

##### TC-026：設置 LLM 提供商為 OpenAI

- ❌ 任務類型識別：預期 `execution`，實際 `query`
- ✅ Agent 調用：預期 `system_config_agent`，實際 `['system_config_agent']`
- ⚠️ 一致性：部分一致（Agent正確，但任務類型錯誤）

##### TC-027：創建新的租戶配置，允許使用 GPT-4

- ✅ 任務類型識別：正確（execution）
- ✅ Agent 調用：預期 `system_config_agent`，實際 `['system_config_agent']`
- ✅ 一致性：一致（**唯一完全通過的場景**）

##### TC-034：生成上週的數據分析報告

- ❌ 任務類型識別：預期 `execution`，實際 `query`
- ❌ Agent 調用：預期 `planning_agent, execution_agent, reports_agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-036：分析上個月的用戶行為，生成報告並發送給管理層

- ❌ 任務類型識別：預期 `complex`，實際 `execution`
- ❌ Agent 調用：預期 `planning_agent, data_agent, reports_agent, execution_agent`，實際 `[]`
- ❌ 一致性：不一致

##### TC-045：編輯配置文件並重啟服務

- ❌ 任務類型識別：預期 `execution`，實際 `query`
- ❌ Agent 調用：預期 `planning_agent, document-editing-agent, execution_agent`，實際 `['system_config_agent']`
- ❌ 一致性：不一致

##### TC-047：更新代碼文件，運行測試，生成測試報告

- ❌ 任務類型識別：預期 `complex`，實際 `execution`
- ❌ Agent 調用：預期 `planning_agent, document-editing-agent, execution_agent, reports_agent`，實際 `[]`
- ❌ 澄清機制：預期需要澄清，實際不需要
- ❌ 一致性：不一致

#### 改進建議優先級

1. **高優先級**：改進 Agent 能力匹配邏輯
   - 影響：14/19 個場景（73.7%）
   - 預期效果：能夠正確識別需要調用的 Agent
   - 建議：增強 `CapabilityMatcher` 的能力匹配邏輯，即使任務被歸類為簡單查詢，也應該識別出需要特定 Agent 的場景

2. **高優先級**：改進任務類型識別
   - 影響：10/19 個場景（52.6%）
   - 預期效果：正確識別 `execution`、`complex`、`log_query` 任務類型
   - 建議：改進 Task Analyzer 的分類邏輯，增強對複雜任務的識別

3. **中優先級**：改進文件編輯場景識別
   - 影響：文件編輯類場景（TC-011 ~ TC-025）
   - 預期效果：當任務包含文件操作關鍵詞時，應該明確建議 `document-editing-agent`
   - 建議：增強對"編輯"、"修改"、"刪除"等關鍵詞的識別

4. **中優先級**：改進複雜任務識別和澄清機制
   - 影響：複雜執行和混合複雜任務
   - 預期效果：正確識別 `complex` 任務類型，改進澄清機制的觸發條件
   - 建議：增強對多步驟任務的識別，改進澄清機制的觸發條件

#### 測試結論

##### 整體評估

- ✅ **意圖解析能力**：系統能夠解析用戶意圖，但任務類型識別需要改進
- ❌ **Agent 調用識別**：這是最大的問題，影響了 14/19 個場景（73.7%）
- ⚠️ **澄清機制**：部分場景的澄清機制需要改進

##### 通過率分析

- **完全通過率**：5.3%（1/19）- 僅 TC-027
- **部分通過率**：36.8%（7/19）
- **失敗率**：57.9%（11/19）

##### 關鍵問題總結

1. **Agent 調用識別**是最嚴重的問題，影響了 14/19 個場景
2. **任務類型識別**是第二嚴重的問題，影響了 10/19 個場景
3. **澄清機制**問題較少，只影響 1/19 個場景

#### 後續測試計劃

1. **立即改進**：
   - 改進 Agent 能力匹配邏輯
   - 改進任務類型識別邏輯

2. **繼續測試**：
   - 完成剩餘的 31 個測試場景
   - 特別關注文件編輯類場景（TC-018, TC-019, TC-021 ~ TC-025）

3. **改進後重新測試**：
   - 根據改進建議修復問題後
   - 重新執行失敗和部分通過的場景

4. **實際執行測試**：
   - 在完成意圖解析測試後
   - 進行實際任務執行測試，驗證端到端流程

---

**測試狀態**：第 1 輪測試完成（19/50 個場景）
**測試完成時間**：2026-01-08 22:33
**測試耗時**：3分38秒
**下次更新**：改進後重新測試或完成剩餘場景測試時
