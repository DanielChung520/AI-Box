# 工具使用指南

**創建日期**: 2025-12-30
**創建人**: Daniel Chung
**最後修改日期**: 2025-12-30

**關聯文檔**: [工具 API 文档](./工具API文档.md)、[工具組開發規格](./工具組開發規格.md)

---

## 📋 概述

本指南提供 AI-Box 工具組的快速開始、常見使用場景、最佳實踐和故障排除指南。

---

## 🚀 快速開始

### 安裝和配置

工具組已包含在 AI-Box 項目中，無需額外安裝。

#### 環境變數配置

某些工具需要 API Key，請設置以下環境變數：

```bash
# 天氣工具（OpenWeatherMap）
export OPENWEATHERMAP_API_KEY="your_api_key_here"

# 貨幣轉換工具（可選，目前使用免費 API）
export EXCHANGE_RATE_API_KEY="your_api_key_here"
```

#### 配置初始化

日期時間工具需要初始化系統配置。運行以下腳本：

```bash
python scripts/init_tools_datetime_config.py
```

### 基本使用流程

#### 1. 導入工具

```python
from tools.time import DateTimeTool, DateTimeInput
from tools.weather import WeatherTool, WeatherInput
from tools.location import IPLocationTool, IPLocationInput
```

#### 2. 創建工具實例

```python
datetime_tool = DateTimeTool()
weather_tool = WeatherTool()
```

#### 3. 執行工具

```python
# 獲取當前日期時間
result = await datetime_tool.execute(DateTimeInput(timezone="Asia/Taipei"))
print(result.datetime)

# 查詢天氣
result = await weather_tool.execute(WeatherInput(city="Taipei"))
print(f"溫度: {result.temperature}°C")
```

### 使用工具註冊表

```python
from tools import register_all_tools, get_tool_registry

# 獲取註冊表
registry = get_tool_registry()

# 註冊所有工具
register_all_tools(registry)

# 獲取工具
tool = registry.get_tool("datetime")
result = await tool.execute(DateTimeInput())
```

---

## 💡 常見使用場景

### 場景 1: 時間日期處理

#### 獲取當前時間

```python
from tools.time import DateTimeTool, DateTimeInput

tool = DateTimeTool()

# 使用默認配置
result = await tool.execute(DateTimeInput())
print(result.datetime)

# 使用自定義時區
result = await tool.execute(DateTimeInput(timezone="Asia/Taipei"))
print(result.datetime)

# 使用自定義格式
result = await tool.execute(DateTimeInput(format="%Y年%m月%d日"))
print(result.datetime)
```

#### 日期計算

```python
from tools.time import DateCalculator, CalculateInput

tool = DateCalculator()

# 計算日期差值
result = await tool.execute(CalculateInput(
    operation="diff",
    date1="2025-01-01",
    date2="2025-12-30"
))
print(f"相差 {result.days_diff} 天")

# 日期加減
result = await tool.execute(CalculateInput(
    operation="add",
    date1="2025-01-01",
    days=30
))
print(f"30 天後: {result.result}")
```

### 場景 2: 天氣查詢

#### 根據城市查詢天氣

```python
from tools.weather import WeatherTool, WeatherInput

tool = WeatherTool()

# 查詢當前天氣
result = await tool.execute(WeatherInput(city="Taipei"))
print(f"城市: {result.city}")
print(f"溫度: {result.temperature}°C")
print(f"天氣: {result.description}")
print(f"濕度: {result.humidity}%")
```

#### 根據經緯度查詢天氣

```python
result = await tool.execute(WeatherInput(
    lat=25.0330,
    lon=121.5654
))
```

#### 獲取天氣預報

```python
from tools.weather import ForecastTool, ForecastInput

tool = ForecastTool()

# 獲取 5 天預報
result = await tool.execute(ForecastInput(
    city="Taipei",
    days=5
))

for forecast in result.forecasts:
    print(f"{forecast.date}: {forecast.temperature}°C")
```

### 場景 3: 地理位置處理

#### IP 地址定位

```python
from tools.location import IPLocationTool, IPLocationInput

tool = IPLocationTool()

result = await tool.execute(IPLocationInput(ip="8.8.8.8"))
print(f"國家: {result.country}")
print(f"城市: {result.city}")
print(f"時區: {result.timezone}")
```

#### 地理編碼

```python
from tools.location import GeocodingTool, GeocodingInput

tool = GeocodingTool()

# 正向編碼（地址 → 坐標）
result = await tool.execute(GeocodingInput(address="Taipei, Taiwan"))
print(f"緯度: {result.latitude}, 經度: {result.longitude}")

# 反向編碼（坐標 → 地址）
result = await tool.execute(GeocodingInput(lat=25.0330, lon=121.5654))
print(f"地址: {result.formatted_address}")
```

#### 距離計算

```python
from tools.location import DistanceTool, DistanceInput

tool = DistanceTool()

# 計算直線距離
result = await tool.execute(DistanceInput(
    lat1=25.0330, lon1=121.5654,  # 台北
    lat2=24.1477, lon2=120.6736,  # 台中
    method="haversine",
    unit="km"
))
print(f"距離: {result.distance} 公里")
```

### 場景 4: 單位轉換

#### 長度轉換

```python
from tools.conversion import LengthConverter, LengthInput

tool = LengthConverter()

result = await tool.execute(LengthInput(
    value=1000.0,
    from_unit="meter",
    to_unit="kilometer"
))
print(f"{result.original_value} 米 = {result.value} 公里")
```

#### 溫度轉換

```python
from tools.conversion import TemperatureConverter, TemperatureInput

tool = TemperatureConverter()

result = await tool.execute(TemperatureInput(
    value=25.0,
    from_unit="celsius",
    to_unit="fahrenheit"
))
print(f"{result.original_value}°C = {result.value}°F")
```

#### 貨幣轉換

```python
from tools.conversion import CurrencyConverter, CurrencyInput

tool = CurrencyConverter()

result = await tool.execute(CurrencyInput(
    amount=100.0,
    from_currency="USD",
    to_currency="TWD"
))
print(f"${result.original_amount} USD = ${result.amount} TWD")
print(f"匯率: {result.exchange_rate}")
```

### 場景 5: 文本處理

#### 文本格式化

```python
from tools.text import TextFormatter, TextFormatterInput

tool = TextFormatter()

result = await tool.execute(TextFormatterInput(
    text="hello world",
    operation="title"
))
print(result.formatted_text)  # "Hello World"
```

#### 文本轉換

```python
from tools.text import TextConverter, TextConverterInput

tool = TextConverter()

# Markdown 轉 HTML
result = await tool.execute(TextConverterInput(
    text="# Title\n\nContent",
    from_format="markdown",
    to_format="html"
))
print(result.converted_text)

# HTML 轉純文本
result = await tool.execute(TextConverterInput(
    text="<h1>Title</h1><p>Content</p>",
    from_format="html",
    to_format="plain"
))
print(result.converted_text)
```

#### 文本摘要

```python
from tools.text import TextSummarizer, TextSummarizerInput

tool = TextSummarizer()

# 提取關鍵詞
result = await tool.execute(TextSummarizerInput(
    text="Python is a programming language...",
    operation="keywords",
    max_keywords=5
))
print(f"關鍵詞: {result.keywords}")

# 生成摘要
result = await tool.execute(TextSummarizerInput(
    text="Long text content...",
    operation="summary",
    summary_length=3
))
print(f"摘要: {result.result}")

# 計算統計信息
result = await tool.execute(TextSummarizerInput(
    text="Sample text",
    operation="stats"
))
print(f"統計: {result.stats}")
```

---

## 🎯 最佳實踐

### 工具選擇建議

1. **時間日期處理**: 使用 `DateTimeTool` 獲取當前時間，使用 `DateCalculator` 進行日期計算
2. **天氣查詢**: 使用 `WeatherTool` 查詢當前天氣，使用 `ForecastTool` 獲取預報
3. **地理位置**: 使用 `IPLocationTool` 進行 IP 定位，使用 `GeocodingTool` 進行地址編碼
4. **單位轉換**: 根據需要選擇對應的轉換工具（長度、重量、溫度等）
5. **文本處理**: 使用 `TextFormatter` 格式化，使用 `TextConverter` 轉換格式，使用 `TextSummarizer` 提取摘要

### 性能優化建議

#### 1. 使用緩存

工具組已為外部 API 調用實現了緩存機制：

- **天氣數據**: 緩存 10 分鐘
- **IP 定位**: 緩存 24 小時
- **地理編碼**: 緩存 7 天
- **貨幣匯率**: 緩存 1 小時

無需手動管理緩存，工具會自動處理。

#### 2. 批量處理

對於需要處理多個數據的情況，使用異步並發：

```python
import asyncio

async def process_multiple_locations():
    tool = GeocodingTool()

    addresses = ["Taipei", "Tokyo", "Seoul"]
    tasks = [
        tool.execute(GeocodingInput(address=addr))
        for addr in addresses
    ]

    results = await asyncio.gather(*tasks)
    return results
```

#### 3. 錯誤處理

始終使用 try-except 處理錯誤：

```python
from tools.utils.errors import ToolExecutionError, ToolValidationError

try:
    result = await tool.execute(input_data)
except ToolValidationError as e:
    print(f"輸入參數錯誤: {e.message}")
except ToolExecutionError as e:
    print(f"工具執行失敗: {e.message}")
except Exception as e:
    print(f"未知錯誤: {e}")
```

### 錯誤處理最佳實踐

#### 1. 驗證輸入參數

在調用工具前驗證輸入參數：

```python
from tools.utils.validator import validate_coordinates, validate_ip_address

# 驗證 IP 地址
if not validate_ip_address(ip):
    raise ValueError("無效的 IP 地址")

# 驗證坐標
if not validate_coordinates(lat, lon):
    raise ValueError("無效的坐標")
```

#### 2. 處理 API 錯誤

對於依賴外部 API 的工具，實現降級策略：

```python
from tools.weather import WeatherTool, WeatherInput
from tools.utils.errors import ToolExecutionError

tool = WeatherTool()

try:
    result = await tool.execute(WeatherInput(city="Taipei"))
except ToolExecutionError:
    # 降級處理：使用緩存數據或返回默認值
    print("天氣 API 不可用，使用緩存數據")
    # ...
```

#### 3. 超時處理

對於網絡請求，設置合理的超時時間（工具已內置超時控制）：

```python
# 工具已內置 10 秒超時，無需手動設置
result = await tool.execute(input_data)
```

### 緩存使用建議

#### 1. 理解緩存策略

不同工具使用不同的緩存策略：

- **天氣數據**: 10 分鐘（數據變化頻繁）
- **IP 定位**: 24 小時（IP 地理位置變化較少）
- **地理編碼**: 7 天（地址不常變化）
- **貨幣匯率**: 1 小時（匯率變化頻繁但 API 限制）

#### 2. 手動清除緩存（如需要）

```python
from tools.utils.cache import get_cache

cache = get_cache()
cache.delete("weather:Taipei")  # 清除特定緩存
cache.clear()  # 清除所有緩存
```

---

## 🔧 故障排除

### 常見問題解答

#### Q1: 天氣工具返回錯誤 "API Key not found"

**問題**: 未設置 `OPENWEATHERMAP_API_KEY` 環境變數。

**解決方案**:

```bash
export OPENWEATHERMAP_API_KEY="your_api_key_here"
```

或在代碼中設置：

```python
import os
os.environ["OPENWEATHERMAP_API_KEY"] = "your_api_key_here"
```

#### Q2: 日期時間工具無法讀取配置

**問題**: 系統配置未初始化。

**解決方案**: 運行配置初始化腳本：

```bash
python scripts/init_tools_datetime_config.py
```

#### Q3: 貨幣轉換工具返回錯誤 "Exchange rate not found"

**問題**: 目標貨幣代碼不支持或 API 響應中缺少該貨幣。

**解決方案**:

1. 檢查貨幣代碼是否正確（使用 ISO 4217 標準）
2. 檢查 API 響應是否包含目標貨幣
3. 嘗試使用其他貨幣代碼

#### Q4: 地理編碼工具返回空結果

**問題**: 地址格式不正確或 API 無法解析。

**解決方案**:

1. 使用更完整的地址（包含國家、城市）
2. 嘗試使用經緯度進行反向編碼
3. 檢查 API 服務是否可用

#### Q5: 工具執行超時

**問題**: 網絡請求超時。

**解決方案**:

1. 檢查網絡連接
2. 檢查 API 服務是否正常
3. 使用緩存數據（如果可用）

### 錯誤代碼說明

#### ToolExecutionError

工具執行失敗，通常由以下原因引起：

- 外部 API 調用失敗
- 網絡連接問題
- 數據處理錯誤

**處理方式**:

```python
try:
    result = await tool.execute(input_data)
except ToolExecutionError as e:
    print(f"工具執行失敗: {e.message}")
    print(f"工具名稱: {e.tool_name}")
```

#### ToolValidationError

輸入參數驗證失敗。

**處理方式**:

```python
try:
    result = await tool.execute(input_data)
except ToolValidationError as e:
    print(f"參數驗證失敗: {e.message}")
    print(f"錯誤字段: {e.field}")
```

#### ToolNotFoundError

工具未找到。

**處理方式**:

```python
from tools.registry import get_tool_registry
from tools.utils.errors import ToolNotFoundError

registry = get_tool_registry()
try:
    tool = registry.get_tool_or_raise("nonexistent_tool")
except ToolNotFoundError as e:
    print(f"工具未找到: {e.tool_name}")
```

#### ToolConfigurationError

工具配置錯誤。

**處理方式**:

```python
try:
    result = await tool.execute(input_data)
except ToolConfigurationError as e:
    print(f"配置錯誤: {e.message}")
    print(f"工具名稱: {e.tool_name}")
```

### 調試技巧

#### 1. 啟用日誌

工具組使用 `structlog` 進行日誌記錄：

```python
import structlog

# 設置日誌級別
structlog.configure(
    processors=[
        structlog.processors.TimeStamper(fmt="iso"),
        structlog.dev.ConsoleRenderer()
    ],
    wrapper_class=structlog.make_filtering_bound_logger(logging.DEBUG),
    context_class=dict,
    logger_factory=structlog.PrintLoggerFactory(),
    cache_logger_on_first_use=False,
)
```

#### 2. 檢查工具狀態

```python
from tools.registry import get_tool_registry

registry = get_tool_registry()

# 列出所有工具
tools = registry.list_tools()
print(f"已註冊工具: {tools}")

# 獲取工具信息
tools_info = registry.list_tools_with_info()
for info in tools_info:
    print(f"{info['name']}: {info['description']} (v{info['version']})")
```

#### 3. 驗證輸入參數

在調用工具前驗證輸入：

```python
from tools.utils.validator import validate_ip_address, validate_coordinates

# 驗證 IP 地址
ip = "8.8.8.8"
if not validate_ip_address(ip):
    print("無效的 IP 地址")

# 驗證坐標
lat, lon = 25.0330, 121.5654
if not validate_coordinates(lat, lon):
    print("無效的坐標")
```

#### 4. 測試工具連接

對於依賴外部 API 的工具，先測試連接：

```python
import httpx

async def test_api_connection():
    try:
        async with httpx.AsyncClient(timeout=5.0) as client:
            response = await client.get("https://api.openweathermap.org/data/2.5/weather")
            print(f"API 狀態: {response.status_code}")
    except Exception as e:
        print(f"API 連接失敗: {e}")
```

### 聯繫支持

如果遇到無法解決的問題，請：

1. 檢查 [工具 API 文档](./工具API文档.md) 確認使用方法
2. 查看項目 Issue 列表
3. 聯繫項目維護者

---

## 📚 相關文檔

- [工具 API 文档](./工具API文档.md) - 完整的 API 說明
- [工具組開發規格](./工具組開發規格.md) - 技術規格和實現細節
- [工具組需求分析](./工具組需求分析.md) - 功能需求說明

---

---

## 🔧 工具註冊清單管理

工具註冊清單存儲在 ArangoDB 中，可以通過 API 進行管理。詳細說明請參閱 [工具註冊清單說明](./工具註冊清單說明.md)。

### 查詢工具註冊信息

```python
from tools.registry_loader import get_tool_info, list_all_tools

# 獲取單個工具信息
tool_info = get_tool_info("datetime")
print(f"工具用途: {tool_info['purpose']}")
print(f"使用場景: {tool_info['use_cases']}")

# 列出所有工具
all_tools = list_all_tools()
for tool in all_tools:
    print(f"{tool['name']}: {tool['description']}")
```

### 通過 API 管理工具註冊清單

```bash
# 查詢所有工具
curl http://localhost:8000/api/v1/tools/registry

# 獲取指定工具信息
curl http://localhost:8000/api/v1/tools/registry/datetime

# 搜索工具
curl "http://localhost:8000/api/v1/tools/registry/search?keyword=日期"
```

---

**最後更新日期**: 2025-12-30
**維護人**: Daniel Chung
