# 工具組需求分析

**創建日期**: 2025-12-30
**創建人**: Daniel Chung
**最後修改日期**: 2025-12-30

**關聯文檔**: [工具組開發規格](./工具組開發規格.md)

---

## 📋 需求概述

### 背景

AI-Box 系統需要一個統一的工具組（Tools）系統，為 Agent 和系統組件提供常用的工具功能。這些工具應該易於擴展、易於使用，並且能夠被 Agent 平台、MCP Server 等組件調用。

### 目標

1. **統一工具接口**：提供統一的工具調用接口，方便 Agent 和系統組件使用
2. **易於擴展**：支持動態註冊新工具，無需修改核心代碼
3. **類型安全**：使用類型注解確保工具參數和返回值的正確性
4. **文檔完善**：每個工具都有完整的文檔說明和使用示例

---

## 🎯 核心需求

### 1. 時間與日期工具（Time & Date Tools）

#### 1.0 時間服務（TimeService）

**需求描述**：

- **緩存機制**：使用緩存機制減少系統調用，每 100ms 更新一次系統時間
- **高精度計算**：使用 `time.perf_counter()` 計算經過的時間，提供微秒級精度
- **線程安全**：支持多線程環境下的安全使用

**設計目標**：

- 提供高精度時間服務，避免系統時間調整導致的時間跳躍
- 通過緩存機制減少系統調用開銷，提高性能
- 簡單實用，無需複雜的同步策略

**使用場景**：

- Agent 需要知道當前準確的日期時間
- 日誌記錄需要高精度時間戳
- 任務調度需要準確的時間
- 時間敏感的操作需要高精度時間

#### 1.1 獲取當前日期時間

**需求描述**：

- 獲取當前系統日期和時間（**使用 TimeService 獲取高精度時間**）
- 支持多種時區
- 支持多種格式輸出（ISO 8601、本地格式、自定義格式）
- **配置管理**：日期格式和時區設置通過 `ConfigStoreService` 存儲在 ArangoDB 中

**配置架構**：

日期格式化配置使用三層配置架構（與系統其他配置一致）：

1. **系統級配置**（`system_configs`）：
   - Scope: `tools.datetime`
   - 存儲默認的日期格式、時區、語言環境
   - 所有用戶共享的默認設置

2. **租戶級配置**（`tenant_configs`）：
   - Scope: `tools.datetime`
   - 租戶特定的格式（如不同地區的日期格式偏好）
   - 可以覆蓋系統級配置

3. **用戶級配置**（`user_configs`）：
   - Scope: `tools.datetime`
   - 用戶個性化格式
   - 優先級最高，可以覆蓋租戶和系統級配置

**配置內容**：

```json
{
  "default_format": "%Y-%m-%d %H:%M:%S",      // 默認日期時間格式
  "default_timezone": "UTC",                   // 默認時區
  "default_locale": "en_US",                  // 默認語言環境
  "iso_format": "%Y-%m-%dT%H:%M:%S%z",        // ISO 8601 格式
  "date_only_format": "%Y-%m-%d",             // 僅日期格式
  "time_only_format": "%H:%M:%S",             // 僅時間格式
  "localized_formats": {                      // 本地化格式（可選）
    "zh_TW": "%Y年%m月%d日 %H:%M:%S",
    "en_US": "%B %d, %Y %I:%M:%S %p"
  }
}
```

**使用場景**：

- Agent 需要知道當前日期來處理時間相關的任務
- 日誌記錄需要時間戳
- 任務調度需要當前時間
- 多租戶環境下不同地區的日期格式需求
- 用戶個性化日期顯示偏好

**功能要求**：

- ✅ 獲取當前日期時間（UTC 和本地時區）
- ✅ 支持時區轉換
- ✅ 支持多種日期格式輸出
- ✅ 支持日期計算（加減天數、月份等）
- ✅ **從 ConfigStoreService 讀取配置**（系統/租戶/用戶三層）
- ✅ **配置優先級**：User > Tenant > System
- ✅ **配置合併**：自動合併三層配置，用戶級配置優先

#### 1.2 日期格式化

**需求描述**：

- 將日期時間格式化為指定格式
- 解析字符串為日期時間對象
- **配置驅動**：格式選項從配置服務讀取

**配置管理**：

日期格式化配置存儲在 ArangoDB 中（Scope: `tools.datetime`），支持：

- **預定義格式**：系統預設的常用格式（ISO 8601、本地格式等）
- **自定義格式**：用戶可以定義自己的格式
- **語言環境**：支持多種語言環境的日期格式（如中文、英文）

**使用場景**：

- 將日期轉換為用戶友好的格式
- 解析用戶輸入的日期字符串
- 根據用戶語言環境自動選擇合適的格式
- 多租戶環境下的格式本地化

**功能要求**：

- ✅ 日期格式化（strftime 格式）
- ✅ 日期解析（從字符串解析）
- ✅ 支持多種語言環境
- ✅ **從配置讀取預定義格式**
- ✅ **支持配置中的本地化格式**

#### 1.3 日期計算

**需求描述**：

- 計算兩個日期之間的差值
- 日期加減運算

**使用場景**：

- 計算任務截止日期
- 計算時間間隔

**功能要求**：

- ✅ 日期差值計算（天數、小時、分鐘等）
- ✅ 日期加減運算
- ✅ 工作日計算（排除週末和節假日）

---

### 2. 天氣工具（Weather Tools）

#### 2.1 獲取當前天氣

**需求描述**：

- 根據地理位置獲取當前天氣信息
- 支持多種天氣 API（OpenWeatherMap、AccuWeather 等）

**使用場景**：

- Agent 需要根據天氣情況調整建議
- 任務執行需要考慮天氣因素

**功能要求**：

- ✅ 根據城市名稱獲取天氣
- ✅ 根據經緯度獲取天氣
- ✅ 支持多種天氣 API
- ✅ 緩存機制（避免頻繁調用 API）
- ✅ 錯誤處理和降級策略

#### 2.2 天氣預報

**需求描述**：

- 獲取未來幾天的天氣預報
- 支持小時級別預報

**使用場景**：

- 計劃戶外活動
- 天氣相關的決策支持

**功能要求**：

- ✅ 獲取未來 3-7 天天氣預報
- ✅ 獲取小時級別預報
- ✅ 天氣趨勢分析

#### 2.3 天氣歷史數據

**需求描述**：

- 獲取歷史天氣數據（可選功能）

**使用場景**：

- 天氣數據分析
- 歷史對比

**功能要求**：

- ⚠️ 可選功能，根據 API 支持情況決定

---

### 3. 地理位置工具（Location Tools）

#### 3.1 IP 地址定位

**需求描述**：

- 根據 IP 地址獲取地理位置信息
- 支持 IPv4 和 IPv6

**使用場景**：

- 根據用戶 IP 獲取地理位置
- 安全審計和日誌分析

**功能要求**：

- ✅ IP 地址到地理位置轉換
- ✅ 支持多種 IP 定位服務（ipapi.co、ip-api.com 等）
- ✅ 緩存機制
- ✅ 隱私保護（不記錄敏感信息）

#### 3.2 地理編碼（Geocoding）

**需求描述**：

- 地址到經緯度轉換（正向地理編碼）
- 經緯度到地址轉換（反向地理編碼）

**使用場景**：

- 將地址轉換為坐標用於地圖顯示
- 根據坐標獲取地址信息

**功能要求**：

- ✅ 正向地理編碼（地址 → 坐標）
- ✅ 反向地理編碼（坐標 → 地址）
- ✅ 支持多種地理編碼服務（Google Maps API、OpenStreetMap 等）
- ✅ 批量處理支持

#### 3.3 距離計算

**需求描述**：

- 計算兩個地理位置之間的距離
- 支持多種距離計算方法（直線距離、駕車距離、步行距離）

**使用場景**：

- 計算配送距離
- 位置相關的推薦

**功能要求**：

- ✅ 直線距離計算（Haversine 公式）
- ✅ 駕車距離計算（需要地圖 API）
- ✅ 步行距離計算（需要地圖 API）
- ✅ 支持多種單位（公里、英里等）

#### 3.4 時區查詢

**需求描述**：

- 根據地理位置獲取時區信息

**使用場景**：

- 時間轉換需要時區信息
- 多時區協作

**功能要求**：

- ✅ 根據坐標獲取時區
- ✅ 根據城市名稱獲取時區
- ✅ 時區偏移量計算

---

## 💡 擴展工具建議

### 4. 單位轉換工具（Unit Conversion Tools）

**需求描述**：

- 長度、重量、溫度、貨幣等單位轉換

**使用場景**：

- 數據處理需要單位轉換
- 國際化支持

**功能要求**：

- ✅ 長度單位轉換（米、英尺、英里等）
- ✅ 重量單位轉換（公斤、磅等）
- ✅ 溫度單位轉換（攝氏度、華氏度、開爾文）
- ✅ 貨幣轉換（需要實時匯率 API）
- ✅ 體積、面積等單位轉換

### 5. 計算工具（Calculator Tools）

**需求描述**：

- 數學計算和表達式求值

**使用場景**：

- Agent 需要進行數學計算
- 數據處理和統計

**功能要求**：

- ✅ 基本數學運算（加減乘除）
- ✅ 科學計算（三角函數、對數等）
- ✅ 表達式求值（安全的 eval）
- ✅ 統計計算（平均值、中位數、標準差等）

### 6. 文本處理工具（Text Processing Tools）

**需求描述**：

- 文本格式化、清理、轉換

**使用場景**：

- 數據清理
- 文本預處理

**功能要求**：

- ✅ 文本清理（去除空白、特殊字符等）
- ✅ 文本格式化（大小寫轉換、首字母大寫等）
- ✅ 文本轉換（Markdown、HTML、純文本互轉）
- ✅ 文本摘要（提取關鍵信息）

### 7. 數據驗證工具（Data Validation Tools）

**需求描述**：

- 數據格式驗證和清理

**使用場景**：

- 輸入驗證
- 數據質量檢查

**功能要求**：

- ✅ 郵箱驗證
- ✅ 電話號碼驗證
- ✅ URL 驗證
- ✅ 身份證號驗證（各國格式）
- ✅ 信用卡號驗證（Luhn 算法）

### 8. 編碼解碼工具（Encoding/Decoding Tools）

**需求描述**：

- 各種編碼格式的轉換

**使用場景**：

- 數據處理
- 安全相關操作

**功能要求**：

- ✅ Base64 編解碼
- ✅ URL 編解碼
- ✅ HTML 實體編解碼
- ✅ JSON 編解碼
- ✅ 哈希計算（MD5、SHA256 等）

### 9. 隨機數生成工具（Random Tools）

**需求描述**：

- 生成隨機數、隨機字符串等

**使用場景**：

- 測試數據生成
- 唯一 ID 生成

**功能要求**：

- ✅ 隨機數生成（整數、浮點數）
- ✅ 隨機字符串生成
- ✅ UUID 生成
- ✅ 隨機選擇（從列表中）

### 10. 文件處理工具（File Tools）

**需求描述**：

- 文件操作和處理

**使用場景**：

- 文件上傳處理
- 文件格式轉換

**功能要求**：

- ✅ 文件類型檢測
- ✅ 文件大小計算
- ✅ 文件哈希計算（MD5、SHA256）
- ✅ 文件壓縮/解壓（ZIP、GZIP 等）

---

## 📊 優先級劃分

### 第一階段（核心工具）

1. **時間與日期工具** - 高優先級
   - 獲取當前日期時間
   - 日期格式化
   - 日期計算

2. **天氣工具** - 高優先級
   - 獲取當前天氣
   - 天氣預報

3. **地理位置工具** - 高優先級
   - IP 地址定位
   - 地理編碼
   - 距離計算

### 第二階段（擴展工具）

4. **單位轉換工具** - 中優先級
5. **計算工具** - 中優先級
6. **文本處理工具** - 中優先級

### 第三階段（高級工具）

7. **數據驗證工具** - 低優先級
8. **編碼解碼工具** - 低優先級
9. **隨機數生成工具** - 低優先級
10. **文件處理工具** - 低優先級

---

## 🔧 技術要求

### 1. 架構要求

- **模組化設計**：每個工具獨立模組，易於維護和擴展
- **統一接口**：所有工具實現統一的接口規範
- **類型安全**：使用 Python 類型注解，支持 mypy 檢查
- **異步支持**：支持異步調用（對於需要網絡請求的工具）

### 2. 性能要求

- **緩存機制**：對於外部 API 調用，實現緩存機制
- **並發支持**：支持並發調用多個工具
- **超時控制**：網絡請求設置超時時間
- **錯誤處理**：完善的錯誤處理和降級策略

### 3. 安全要求

- **輸入驗證**：所有輸入參數必須驗證
- **API 密鑰管理**：敏感信息（API Key）加密存儲
- **隱私保護**：不記錄敏感信息（如 IP 地址、位置信息）
- **權限控制**：支持工具級別的權限控制

### 4. 文檔要求

- **API 文檔**：每個工具都有完整的 API 文檔
- **使用示例**：提供使用示例和最佳實踐
- **錯誤處理**：文檔說明可能的錯誤和處理方法

---

## 📝 使用場景示例

### 場景 1：Agent 需要當前日期

```python
from tools.time import get_current_datetime

# 獲取當前日期時間
current_time = await get_current_datetime(timezone="Asia/Taipei")
print(f"當前時間：{current_time}")
```

### 場景 2：根據天氣調整建議

```python
from tools.weather import get_current_weather

# 獲取天氣信息
weather = await get_current_weather(city="Taipei")
if weather.temperature > 30:
    print("天氣很熱，建議多喝水")
```

### 場景 3：計算距離

```python
from tools.location import calculate_distance

# 計算兩個地點之間的距離
distance = await calculate_distance(
    lat1=25.0330, lon1=121.5654,  # 台北
    lat2=24.1477, lon2=120.6736,  # 台中
    unit="km"
)
print(f"距離：{distance} 公里")
```

---

## ✅ 驗收標準

### 功能驗收

- [ ] 所有核心工具（時間、天氣、地理位置）實現完成
- [ ] 所有工具都有完整的單元測試（覆蓋率 > 80%）
- [ ] 所有工具都有完整的 API 文檔
- [ ] 所有工具都通過類型檢查（mypy）

### 性能驗收

- [ ] 工具調用響應時間 < 1 秒（本地工具）
- [ ] 外部 API 調用有緩存機制（避免重複調用）
- [ ] 支持並發調用（至少 10 個並發請求）

### 安全驗收

- [ ] 所有輸入參數都有驗證
- [ ] API 密鑰加密存儲
- [ ] 敏感信息不記錄到日誌

---

## 📅 開發計劃

### Phase 1: 核心工具實現（1-2 週）

- 時間與日期工具
- 天氣工具（基礎功能）
- 地理位置工具（基礎功能）

### Phase 2: 擴展功能（1 週）

- 天氣預報
- 距離計算
- 時區查詢

### Phase 3: 擴展工具（2-3 週）

- 單位轉換工具
- 計算工具
- 文本處理工具

### Phase 4: 測試與文檔（1 週）

- 單元測試
- 集成測試
- 文檔完善

---

**配置管理說明**：

⚠️ **重要**：日期格式化配置**不存儲在 `config.json` 文件中**，而是通過 `ConfigStoreService` 存儲在 ArangoDB 中。

**原因**：

1. **多租戶支持**：不同租戶可能有不同的日期格式偏好（如台灣使用 `%Y年%m月%d日`，美國使用 `%B %d, %Y`）
2. **用戶個性化**：用戶可以設置自己的日期格式偏好
3. **動態配置**：可以在運行時修改配置，無需重啟服務
4. **配置統一管理**：與系統其他配置（如 `genai.policy`、`llm.provider_config`）使用相同的配置架構
5. **配置優先級**：支持 User > Tenant > System 的配置合併邏輯

**配置存儲位置**：

- **ArangoDB Collections**：
  - `system_configs`：系統級默認配置（Scope: `tools.datetime`）
  - `tenant_configs`：租戶級配置（Scope: `tools.datetime`）
  - `user_configs`：用戶級配置（Scope: `tools.datetime`）
- **讀取方式**：通過 `ConfigStoreService.get_effective_config()` 自動合併三層配置

**與其他配置的一致性**：

系統中其他配置也使用相同的架構：

- `genai.policy`：GenAI 策略配置
- `genai.model_registry`：模型註冊表配置
- `llm.provider_config`：LLM 提供商配置
- `tools.datetime`：日期時間工具配置（新增）

---

**最後更新**: 2025-12-30
**維護人**: Daniel Chung
