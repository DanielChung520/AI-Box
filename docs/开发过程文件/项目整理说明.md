# 項目文件整理說明

**整理日期**: 2026-01-05  
**整理人**: Daniel Chung

---

## 📋 整理目標

將根目錄下的測試代碼、工具腳本、診斷腳本、測試結果和報告文件整理到相應的目錄，保持根目錄整潔有序。

---

## 📁 文件移動記錄

### 1. 工具腳本 → `scripts/tools/`

以下工具腳本已移至 `scripts/tools/` 目錄：

- `check_code_quality.py` - 自動化代碼質量檢查腳本
- `check_docs_references.py` - 文檔引用檢查腳本
- `convert_code_registry_to_excel.py` - 代碼註冊表轉換為 Excel
- `generate_code_registry.py` - 生成代碼註冊表
- `sync_tools_via_api.py` - 通過 API 同步工具
- `update_related_files_in_excel.py` - 更新 Excel 中的相關文件
- `update_tools_code_registry_full.py` - 更新工具代碼註冊表（完整版）
- `vectorize_system_docs.py` - 向量化系統文檔
- `import_ontology.py` - 將 Ontology JSON 文件導入到 ArangoDB

**用途**: 這些腳本用於代碼質量檢查、文檔管理、工具註冊表維護等開發輔助工作。

### 2. 診斷腳本 → `scripts/diagnosis/`

以下診斷腳本已移至 `scripts/diagnosis/` 目錄：

- `diagnose_kg_extraction.py` - 診斷圖譜提取問題
- `diagnose_ner_extraction.py` - 診斷 NER 提取問題
- `monitor_processing.py` - 監控處理進度

**用途**: 這些腳本用於診斷和監控系統運行狀態，幫助排查問題。

### 3. DateTime 工具測試 → `tests/datetime_tools/`

以下測試文件已移至 `tests/datetime_tools/` 目錄：

- `test_datetime_intent.py` - 測試 DateTimeTool 意圖識別和執行流程
- `test_datetime_intent_simple.py` - 簡化版測試（不需要數據庫連接）
- `test_datetime_logic.py` - 測試 DateTimeTool 意圖識別邏輯

**用途**: 這些測試文件用於測試時間相關的工具功能。

### 4. 需求規格測試 → `tests/requirement_spec/`

以下測試文件已移至 `tests/requirement_spec/` 目錄：

- `test_requirement_spec.log` - 需求規格測試日誌
- `test_requirement_spec_result.json` - 需求規格測試結果
- `test_files_3.txt` - 測試文件列表

**用途**: 這些文件用於需求規格相關的測試。

### 5. 歷史測試報告 → `docs/测试报告/历史报告/`

以下歷史報告文件已移至 `docs/测试报告/历史报告/` 目錄：

- `ERR_1579修复与数据重新生成完整执行报告.json`
- `ERR_1579修复与数据重新生成完整执行报告_修正版.json`
- `ERR_1579修复与数据重新生成完整执行报告_最终版.json`
- `ERR_1579修复与数据重新生成完整报告.json`
- `ERR_1579修复与数据重新生成报告.json`

**用途**: 這些文件記錄了 ERR_1579 修復和數據重新生成的歷史報告，用於參考和審計。

### 6. 測試執行結果 → `docs/测试报告/执行结果/`

以下測試執行結果文件已移至 `docs/测试报告/执行结果/` 目錄：

- `phase2_batch_test_results.json` - Phase 2 批量測試結果
- `phase2_test_output.log` - Phase 2 測試輸出日誌
- `regenerate_graphs.log` - 重新生成圖譜日誌
- `regenerate_graphs.pid` - 重新生成圖譜進程 ID
- `regenerate_graphs_execution.log` - 重新生成圖譜執行日誌
- `regenerate_graphs_execution_v2.log` - 重新生成圖譜執行日誌 v2
- `regenerate_graphs_results.json` - 重新生成圖譜結果
- `regenerate_graphs_v2.pid` - 重新生成圖譜進程 ID v2
- `regenerate_vectors.log` - 重新生成向量日誌
- `regenerate_vectors_execution.log` - 重新生成向量執行日誌
- `regenerate_vectors_execution_v2.log` - 重新生成向量執行日誌 v2
- `regenerate_vectors_results.json` - 重新生成向量結果
- `system_docs_processing_progress.json` - 系統文檔處理進度
- `system_docs_processing_results.json` - 系統文檔處理結果
- `verification_report_final.json` - 驗證報告（最終版）
- `batch_test_100.log` - 批量測試日誌
- `batch_test_100.pid` - 批量測試進程 ID
- `failed_files_to_retry.txt` - 需要重試的失敗文件列表
- `files_needing_vectors.json` - 需要向量化的文件列表

**用途**: 這些文件記錄了各種測試和批處理操作的執行結果和進度。

### 7. 測試結果文件 → `tests/文件上傳/`

以下測試結果文件已移至 `tests/文件上傳/` 目錄：

- `test_results_gemini.json` - Gemini 模型測試結果
- `test_results_ollama.json` - Ollama 模型測試結果
- `test_results_ollama_32k.json` - Ollama 32k 模型測試結果
- `test_results_ollama_clean.json` - Ollama Clean 模型測試結果
- `test_results_ollama_concurrency10.json` - Ollama 併發 10 測試結果
- `test_results_ollama_concurrency4.json` - Ollama 併發 4 測試結果
- `test_results_ollama_final.json` - Ollama 最終測試結果

**用途**: 這些文件記錄了不同模型的測試結果，用於模型性能對比。

### 8. 備份文件 → `backup/`

以下備份文件已移至 `backup/` 目錄：

- `coverage.json` - 代碼覆蓋率報告
- `requirements.txt.bak` - requirements.txt 備份

**用途**: 這些文件是備份數據，保留用於參考。

---

## 📂 根目錄保留文件

以下文件保留在根目錄，因為它們是項目的核心配置文件：

### 配置文件
- `pyproject.toml` - Python 項目配置文件
- `pytest.ini` - pytest 配置文件
- `requirements.txt` - Python 依賴列表
- `requirements-dev.txt` - 開發環境依賴列表
- `docker-compose.yml` - Docker Compose 配置文件
- `docker-compose.seaweedfs.yml` - SeaweedFS Docker Compose 配置
- `docker-compose.seaweedfs-datalake.yml` - SeaweedFS DataLake Docker Compose 配置
- `env.example` - 環境變數示例文件
- `external_mcp_tools.yaml.example` - 外部 MCP 工具配置示例

### 文檔文件
- `QUICK_START.md` - 快速開始指南
- `UPLOAD_INSTRUCTIONS.md` - 上傳說明文檔
- `AI_開發規範檢查清單.md` - AI 開發規範檢查清單

### 項目文件
- `Dockerfile` - Docker 構建文件
- `SmartQ.-logo.svg` - 項目 Logo
- `AI-Box.code-workspace` - VS Code 工作區配置文件

### 其他
- `.gitignore` - Git 忽略文件配置
- `README.md` - 項目說明文檔（如果存在）

---

## 📋 目錄結構

整理後的目錄結構如下：

```
AI-Box/
├── scripts/
│   ├── tools/                    # 工具腳本
│   │   ├── check_code_quality.py
│   │   ├── check_docs_references.py
│   │   ├── convert_code_registry_to_excel.py
│   │   ├── generate_code_registry.py
│   │   ├── sync_tools_via_api.py
│   │   ├── update_related_files_in_excel.py
│   │   ├── update_tools_code_registry_full.py
│   │   ├── vectorize_system_docs.py
│   │   └── import_ontology.py
│   └── diagnosis/                # 診斷腳本
│       ├── diagnose_kg_extraction.py
│       ├── diagnose_ner_extraction.py
│       └── monitor_processing.py
│
├── tests/
│   ├── 文件上傳/                 # 文件上傳測試（已整理）
│   │   ├── test_*.py
│   │   ├── update_kg_model_*.py
│   │   ├── *.log
│   │   ├── *.json
│   │   └── *.md
│   ├── datetime_tools/           # DateTime 工具測試
│   │   ├── test_datetime_intent.py
│   │   ├── test_datetime_intent_simple.py
│   │   └── test_datetime_logic.py
│   └── requirement_spec/         # 需求規格測試
│       ├── test_requirement_spec.log
│       ├── test_requirement_spec_result.json
│       └── test_files_3.txt
│
├── docs/
│   └── 测试报告/
│       ├── 历史报告/             # 歷史測試報告
│       │   └── ERR_1579*.json
│       └── 执行结果/             # 測試執行結果
│           ├── phase2_*.json
│           ├── regenerate_*.log
│           ├── regenerate_*.json
│           ├── system_docs_*.json
│           └── ...
│
├── backup/                       # 備份文件
│   ├── coverage.json
│   └── requirements.txt.bak
│
└── [根目錄配置文件]              # 保留在根目錄
    ├── pyproject.toml
    ├── pytest.ini
    ├── requirements.txt
    ├── docker-compose*.yml
    └── ...
```

---

## 🔍 查找文件指南

### 工具腳本
所有工具腳本現在位於 `scripts/tools/` 目錄。

### 診斷腳本
所有診斷腳本現在位於 `scripts/diagnosis/` 目錄。

### 測試文件
- **文件上傳測試**: `tests/文件上傳/`
- **DateTime 工具測試**: `tests/datetime_tools/`
- **需求規格測試**: `tests/requirement_spec/`

### 測試報告
- **歷史報告**: `docs/测试报告/历史报告/`
- **執行結果**: `docs/测试报告/执行结果/`

### 備份文件
所有備份文件位於 `backup/` 目錄。

---

## 📝 維護建議

1. **新增工具腳本**: 請放在 `scripts/tools/` 目錄
2. **新增診斷腳本**: 請放在 `scripts/diagnosis/` 目錄
3. **新增測試文件**: 請根據測試類型放在 `tests/` 相應子目錄
4. **新增測試報告**: 請放在 `docs/测试报告/` 相應子目錄
5. **定期清理**: 建議定期清理舊的日誌和結果文件

---

**最後更新日期**: 2026-01-05

