# AI-Bot 前端環境配置範例
# 複製此文件為 .env 並根據實際情況修改配置

# 後端 API 基礎 URL
VITE_API_BASE_URL=http://localhost:8000

# API 前綴（如果後端 API 有前綴，例如 /api/v1）
VITE_API_PREFIX=/api/v1

# HMR WebSocket 配置（用於代理環境，如 Cloudflare Tunnel）
# 當通過代理訪問時（如 bot.k84.org），需要配置以下選項：
# VITE_HMR_CLIENT_HOST=bot.k84.org  # 客戶端連接的主機名（代理域名）
# VITE_HMR_PROTOCOL=wss              # WebSocket 協議（wss 用於 HTTPS，ws 用於 HTTP）
# VITE_HMR_CLIENT_PORT=443           # 客戶端連接的端口（443 用於 HTTPS，80 用於 HTTP）
# 如果直接訪問 localhost，則不需要配置這些選項
