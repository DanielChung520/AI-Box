# 代碼功能說明: AI-Bot 前端環境配置
# 創建日期: 2025-12-03
# 創建人: Daniel Chung
# 最後修改日期: 2025-01-27

# 後端 API 基礎 URL
# 注意：如果前端部署在 HTTPS 域名上（如 bot.k84.org），
# 後端也應該通過相同的域名訪問，避免 CORS 問題
# 如果後端部署在 bot.k84.org 上，使用 https://bot.k84.org
# 如果後端仍在 localhost:8000，需要配置反向代理或隧道
VITE_API_BASE_URL=https://bot.k84.org

# API 前綴（如果後端 API 有前綴，例如 /api/v1）
VITE_API_PREFIX=/api/v1

# HMR WebSocket 配置（用於通過 Cloudflare Tunnel 訪問時）
# 取消下面的註釋並配置正確的值以修復 WebSocket 連接問題
VITE_HMR_CLIENT_HOST=bot.k84.org
VITE_HMR_PROTOCOL=wss
VITE_HMR_CLIENT_PORT=443

# 如果 WebSocket 連接仍然失敗，可以設置為 true 來禁用 HMR（不推薦，會失去熱更新功能）
# VITE_HMR_DISABLE=false
